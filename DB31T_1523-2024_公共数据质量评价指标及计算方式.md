# DB31/T 1523-2024 公共数据质量评价指标及计算方式

## 1. 文档说明

- 来源：DB31/T 1523-2024《公共数据质量评价要求》。
- 指标来源：第 5 章表 2 至表 7。
- 汇总方法来源：第 6 章公式（1）至公式（3）。
- PDF 定位：指标正文位于 PDF 第 10 至 15 页（标准正文页码 4 至 9），资料性附录 A 的示例位于 PDF 第 18 页（标准正文页码 12）。
- 指标体系包括 6 个一级指标、23 个二级指标和 7 个三级指标。
- 本文以正文表 2 至表 7 的指标名称和代码为准。资料性附录 A 中与正文不一致的代码见第 6 节。

## 2. 基础计算约定

### 2.1 正向比例指标

大多数指标采用以下计算方式：

$$
X=\frac{A}{B}
$$

其中：

- $X$：指标得分；
- $A$：满足该项质量要求的元素或数据个数；
- $B$：被评价或预期应被赋值的元素个数。

### 2.2 反向问题指标

“数据重复率”和“脏数据出现率”采用反向计分：

$$
X=1-\frac{A}{B}
$$

其中 $A$ 是重复元素或脏数据元素的个数。因此，重复或脏数据越少，指标得分越高。

### 2.3 子指标平均

“数据标准”和“关联数据一致性”由三级指标等权平均得到：

$$
X_{\text{数据标准}}=\frac{A+B+C}{3}
$$

$$
X_{\text{关联数据一致性}}=\frac{A+B+C+D}{4}
$$

上述字母分别代表对应三级指标的得分，具体映射见后文。

## 3. 指标及计算方式

### 3.1 公共数据规范性（一级代码 01）

公共数据规范性衡量数据符合数据标准、数据模型、元数据、业务规则、权威参考数据和安全规范的程度。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 010100 | 二级 | 数据标准 | 数据集中数据符合数据标准的程度。数据标准可包括参考数据和主数据、数据元和指标数据，范围包括国际、国家、行业、地方和团体标准等。 | $X=(A+B+C)/3$。$A$ 为数据类型约束规范性得分；$B$ 为数据格式约束规范性得分；$C$ 为数据长度约束规范性得分。 |
| 010101 | 三级，隶属 010100 | 数据类型约束规范性 | 数据集中数据类型符合数据标准的程度。 | $X=A/B$。$A$ 为满足数据类型约束的元素个数；$B$ 为被评价元素个数。 |
| 010102 | 三级，隶属 010100 | 数据格式约束规范性 | 数据集中数据格式符合数据标准的程度。 | $X=A/B$。$A$ 为满足数据格式约束的元素个数；$B$ 为被评价元素个数。 |
| 010103 | 三级，隶属 010100 | 数据长度约束规范性 | 数据集中数据长度符合数据标准的程度，适用于结构化数据。 | $X=A/B$。$A$ 为满足数据长度约束的元素个数；$B$ 为被评价元素个数。 |
| 010200 | 二级 | 数据模型 | 数据集中数据符合数据模型的程度，适用于结构化数据。 | $X=A/B$。$A$ 为满足数据模型要求的元素个数；$B$ 为被评价元素个数。 |
| 010300 | 二级 | 元数据 | 数据集中数据符合元数据定义的程度。 | $X=A/B$。$A$ 为满足元数据定义的元素个数；$B$ 为被评价元素个数。 |
| 010400 | 二级 | 业务规则 | 数据集中数据符合各公共服务业务规则的程度，适用于结构化数据。 | $X=A/B$。$A$ 为满足业务规则的元素个数；$B$ 为被评价元素个数。 |
| 010500 | 二级 | 权威参考数据（权威参考源） | 数据集中数据符合权威参考数据的程度。 | $X=A/B$。$A$ 为满足参考数据规则的元素个数；$B$ 为被评价元素个数。 |
| 010600 | 二级 | 安全规范 | 数据集中数据符合安全和隐私规则的程度，包括数据权限管理、数据脱敏处理等要求。 | $X=A/B$。$A$ 为满足安全规范的元素个数；$B$ 为被评价元素个数。 |

### 3.2 公共数据完整性（一级代码 02）

公共数据完整性衡量按照业务规则要求，应被赋值的数据元素和数据记录是否填写完整、无缺失。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 020100 | 二级 | 数据元素完整性 | 按照业务规则要求，应被赋值的数据元素填写完整、无缺失的程度，即数据元素的赋值程度。 | $X=A/B$。$A$ 为被赋值元素的个数；$B$ 为预期被赋值元素的个数。 |
| 020200 | 二级 | 数据记录完整性 | 按照业务规则要求，应被赋值的数据记录填写完整、无缺失的程度，即数据记录的赋值程度。 | $X=A/B$。按标准原文，$A$ 为被赋值元素的个数；$B$ 为预期被赋值元素的个数。 |

### 3.3 公共数据准确性（一级代码 03）

公共数据准确性衡量数据描述的实体与真实值相符的程度，包括内容正确、格式合规、重复、唯一、脏数据以及权威参照一致情况。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 030100 | 二级 | 数据内容正确性 | 数据集中数据内容是否为预期数据，适用于结构化数据。 | $X=A/B$。$A$ 为满足数据正确性要求的元素个数；$B$ 为被评价元素个数。 |
| 030200 | 二级 | 数据格式合规性 | 数据格式，包括数据类型、数值范围、数据长度和精度等，是否满足预期要求。 | $X=A/B$。按标准原文，$A$ 为满足业务精度需求的元素个数；$B$ 为被评价元素个数。 |
| 030300 | 二级 | 数据重复率 | 数据集中特定字段、记录、文件或数据集意外重复的程度。 | $X=1-A/B$。$A$ 为重复元素的个数；$B$ 为被评价元素个数。 |
| 030400 | 二级 | 数据唯一性 | 数据集中特定字段、记录、文件或数据集保持唯一的程度。 | $X=A/B$。$A$ 为满足唯一性要求的元素个数；$B$ 为被评价元素个数。 |
| 030500 | 二级 | 脏数据出现率 | 数据集中正确字段、记录、文件或数据集之外无效数据的程度。 | $X=1-A/B$。$A$ 为出现脏数据的元素个数；$B$ 为被评价元素个数。 |
| 030600 | 二级 | 数据标准参照准确性 | 数据集中的数据与明确权威数据参照是否一致。 | $X=A/B$。$A$ 为在存在明确权威数据参照时，与参照保持一致的元素个数；$B$ 为被评价元素个数。 |

### 3.4 公共数据一致性（一级代码 04）

公共数据一致性衡量数据在不同存储位置、应用、用户、表或上下文中是否保持一致、无矛盾。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 040100 | 二级 | 相同数据一致性 | 同一数据在不同位置存储或被不同应用、用户使用时的一致性；数据变化时，不同位置的同一数据应同步修改。 | $X=A/B$。$A$ 为满足数据源一致性的数据个数；$B$ 为被评价元素个数。 |
| 040200 | 二级 | 关联数据一致性 | 根据一致性约束规则检查关联数据的一致性。 | $X=(A+B+C+D)/4$。$A$ 为表内等值一致性得分；$B$ 为表内逻辑一致性得分；$C$ 为跨表等值一致性得分；$D$ 为跨表逻辑一致性得分。 |
| 040201 | 三级，隶属 040200 | 表内等值一致性 | 对于单表而言，跨列元素值的关系是否保持一致，适用于结构化数据。 | $X=A/B$。$A$ 为满足表内跨列等值一致性的数据个数；$B$ 为被评价元素个数。 |
| 040202 | 三级，隶属 040200 | 表内逻辑一致性 | 对于单表而言，跨列元素值的逻辑关系是否保持一致，适用于结构化数据。 | $X=A/B$。$A$ 为满足表内跨列逻辑一致性的数据个数；$B$ 为被评价元素个数。 |
| 040203 | 三级，隶属 040200 | 跨表等值一致性 | 对于跨表而言，元素值的关系是否保持一致，适用于结构化数据。 | $X=A/B$。$A$ 为满足跨表等值一致性的数据个数；$B$ 为被评价元素个数。 |
| 040204 | 三级，隶属 040200 | 跨表逻辑一致性 | 对于跨关系表而言，元素值的逻辑是否保持一致，适用于结构化数据。 | $X=A/B$。$A$ 为满足跨表逻辑一致性的数据个数；$B$ 为被评价元素个数。 |
| 040300 | 二级 | 内容数据记录数据项与元数据一致性 | 数据集中内容数据记录的数据项与元数据保持一致。 | $X=A/B$。$A$ 为内容数据记录数据项与元数据一致的数据个数；$B$ 为被评价元素个数。 |

### 3.5 公共数据时效性（一级代码 05）

公共数据时效性衡量数据随时间变化时的正确性、及时性、时序关系以及授权使用有效期。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 050100 | 二级 | 基于时间段的正确性 | 基于日期范围的记录数或频率分布符合业务需求的程度。 | $X=A/B$。按标准原文，$A$ 为满足有效性要求的元素个数；$B$ 为被评价元素个数。 |
| 050200 | 二级 | 基于时间点的及时性 | 具有时效性的数据应及时公开或提供，公开的数据处于有效期限内。 | $X=A/B$。$A$ 为满足及时性要求的元素个数；$B$ 为被评价元素个数。 |
| 050300 | 二级 | 时序性 | 数据集中同一实体的数据元素之间的相对时序关系，适用于结构化数据。 | $X=A/B$。$A$ 为满足时序性要求的元素个数；$B$ 为被评价元素个数。 |
| 050400 | 二级 | 数据授权使用时效性 | 基于数据授权有效周期提供数据使用。 | $X=A/B$。$A$ 为满足使用时效性要求的元素个数；$B$ 为被评价元素个数。 |

### 3.6 公共数据可访问性（一级代码 06）

公共数据可访问性衡量数据能否在需要时取得，以及数据是否适配应用场景并可被使用。

| 指标代码 | 层级 | 指标名称 | 指标释义 | 计算方式 |
|---|---|---|---|---|
| 060100 | 二级 | 可访问 | 数据在需要时间的可获取性。 | $X=A/B$。$A$ 为满足可访问性要求的元素个数；$B$ 为被评价元素个数。 |
| 060200 | 二级 | 可用性 | 数据适配应用场景的可使用性。 | $X=A/B$。$A$ 为满足可用性要求的元素个数；$B$ 为被评价元素个数。 |

## 4. 分级加权汇总方法

### 4.1 评价对象最终得分

$$
P=\sum_{i=1}^{6}(R_i\times X_i)
$$

其中：

- $P$：评价对象最终得分；
- $i$：第 $i$ 个一级指标；
- $R_i$：第 $i$ 个一级指标的实际得分；
- $X_i$：第 $i$ 个一级指标的权重。

### 4.2 一级指标得分

$$
R_i=\sum_{j=a}^{n}(S_{ij}\times Y_{ij})
$$

其中：

- $j$：第 $j$ 个二级指标；
- $a$：第 $i$ 个一级指标项下的起始二级指标序号；
- $n$：第 $i$ 个一级指标项下的结尾二级指标序号；
- $S_{ij}$：第 $i$ 个一级指标项下，第 $j$ 个二级指标的实际得分；
- $Y_{ij}$：第 $i$ 个一级指标项下，第 $j$ 个二级指标的权重。

### 4.3 二级指标得分

当二级指标下设三级指标时：

$$
S_{ij}=\sum_{k=b}^{m}(T_{ijk}\times Z_{ijk})
$$

其中：

- $k$：第 $k$ 个三级指标；
- $b$：第 $j$ 个二级指标项下的起始三级指标序号；
- $m$：第 $j$ 个二级指标项下的结尾三级指标序号；
- $T_{ijk}$：第 $j$ 个二级指标项下，第 $k$ 个三级指标的实际得分；
- $Z_{ijk}$：第 $j$ 个二级指标项下，第 $k$ 个三级指标的权重。

未设置三级指标的二级指标，其 $S_{ij}$ 直接采用该二级指标按第 3 节公式计算得到的分值。

### 4.4 权重设置

标准允许评价方依据下列因素设置各级指标权重：

- 数据在业务中的重要程度；
- 数据调用频次；
- 数据访问情况；
- 数据应用场景；
- 数据质量问题的影响程度。

实施时宜对同一父指标下的子指标权重进行归一化，使其权重之和为 1。

## 5. 实施口径建议

1. 标准表 2 至表 7 的基础公式直接产生 0 至 1 的比例值；若项目采用百分制，可在统一口径下将基础得分乘以 100 后再进行加权汇总。资料性附录 A 的示例采用百分制。
2. 标准未规定分母 $B=0$ 时的处理方式。实施方案应预先约定“不适用”“不计权重”或其他处理规则，避免除零。
3. 应在评价开始前明确“元素”的统计粒度，例如字段值、数据记录、文件或数据集，且同一指标内保持一致。
4. 对不适用于评价对象的指标，应明确其权重调整方法，避免直接记 0 分造成误判。
5. 质量评价指标宜完整覆盖本指标体系；评价方可结合实际业务需要增加个性化指标。
6. “数据重复率”和“脏数据出现率”的名称描述的是问题率，但标准给出的 $X$ 是质量得分，即问题越少，得分越高。

## 6. 原文及附录中的不一致说明

以下差异不改变本文按正文表 2 至表 7 整理的指标体系：

1. 正文表 2 中，`010103 数据长度约束规范性` 的变量说明沿用了“数据格式约束规范性”的表述。本文依据指标名称、指标释义和示例，将 $A$ 解释为满足数据长度约束的元素个数。
2. 正文表 3 对 `020200 数据记录完整性` 的 $A$、$B$ 仍使用“元素个数”表述，本文保留原文。实际实施时应明确按记录还是按必填元素统计。
3. 正文表 4 中：

   - `030400` 为“数据唯一性”；
   - `030500` 为“脏数据出现率”。
   
   资料性附录 A 的表 A.1 将 `030500` 标为“数据唯一性”，与正文不一致。
4. 正文表 5 中：

   - `040200` 为“关联数据一致性”；
   - 其三级指标代码为 `040201` 至 `040204`。
   
   资料性附录 A 的表 A.1 使用了 `040100`、`040101` 和 `040104` 等不同代码，与正文不一致。

## 7. 快速索引

| 一级指标 | 二级指标数量 | 三级指标数量 |
|---|---:|---:|
| 01 公共数据规范性 | 6 | 3 |
| 02 公共数据完整性 | 2 | 0 |
| 03 公共数据准确性 | 6 | 0 |
| 04 公共数据一致性 | 3 | 4 |
| 05 公共数据时效性 | 4 | 0 |
| 06 公共数据可访问性 | 2 | 0 |
| **合计** | **23** | **7** |

