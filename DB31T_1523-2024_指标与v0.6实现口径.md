# DB31/T 1523-2024 指标与 v0.6 实现口径

## 1. 范围

本文件记录 DB31/T 1523-2024《公共数据质量评价要求》正文表 2 至表 7 的 30 项指标，以及本 Demo v0.6 的稳定指标 ID、计算方式和当前自动评价能力。

v0.6 直接在 v0.4 上开发。原 v0.4 的 13 项指标全部保留，DB31/T 的 30 项指标作为独立定义加入，两者即使含义接近也不会去重。完整可选目录共 43 项。

标准中的比例结果均按 0 至 1 表示。所有 DB31/T 指标都是“越高越好”的质量得分；其中“数据重复率”和“脏数据出现率”虽然名称描述问题率，标准公式仍使用 `1 - A/B` 生成质量得分。

## 2. 基础公式

- 正向比例：`X = A / B`。
- 反向问题比例：`X = 1 - A / B`。
- 数据标准：`X = (010101 + 010102 + 010103) / 3`。
- 关联数据一致性：`X = (040201 + 040202 + 040203 + 040204) / 4`。
- 分母 `B = 0` 时不以 0 代替，结果记为“无法评估”。

## 3. 指标目录

### 3.1 公共数据规范性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 010100 | `db31_010100` | 二级 | 数据标准 | `X=(A+B+C)/3`；A、B、C 分别为 010101、010102、010103 得分 | 需数据类型、格式和长度标准 |
| 010101 | `db31_010101` | 三级 | 数据类型约束规范性 | `X=A/B`；A 为满足类型约束的元素数，B 为被评价元素数 | 需逐字段预期类型 |
| 010102 | `db31_010102` | 三级 | 数据格式约束规范性 | `X=A/B`；A 为满足格式约束的元素数，B 为被评价元素数 | 需逐字段格式标准 |
| 010103 | `db31_010103` | 三级 | 数据长度约束规范性 | `X=A/B`；A 为满足长度约束的元素数，B 为被评价元素数 | 需长度及计量规则 |
| 010200 | `db31_010200` | 二级 | 数据模型 | `X=A/B`；A 为满足数据模型要求的元素数，B 为被评价元素数 | 需目标模型定义 |
| 010300 | `db31_010300` | 二级 | 元数据 | `X=A/B`；A 为满足元数据定义的元素数，B 为被评价元素数 | 需独立元数据定义 |
| 010400 | `db31_010400` | 二级 | 业务规则 | `X=A/B`；A 为满足业务规则的元素数，B 为被评价元素数 | 需经确认的业务规则 |
| 010500 | `db31_010500` | 二级 | 权威参考数据（权威参考源） | `X=A/B`；A 为满足参考数据规则的元素数，B 为被评价元素数 | 需权威参照、匹配键和规则 |
| 010600 | `db31_010600` | 二级 | 安全规范 | `X=A/B`；A 为满足安全规范的元素数，B 为被评价元素数 | 需分类分级、权限、脱敏及隐私规则 |

### 3.2 公共数据完整性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 020100 | `db31_020100` | 二级 | 数据元素完整性 | `X=A/B`；A 为被赋值元素数，B 为预期被赋值元素数 | 需应赋值元素或必填规则 |
| 020200 | `db31_020200` | 二级 | 数据记录完整性 | `X=A/B`；标准原文仍以被赋值和预期被赋值元素描述 A、B | 需预期记录总体、业务键或应有记录清单 |

### 3.3 公共数据准确性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 030100 | `db31_030100` | 二级 | 数据内容正确性 | `X=A/B`；A 为满足正确性要求的元素数，B 为被评价元素数 | 需真实值、预期值或正确性规则 |
| 030200 | `db31_030200` | 二级 | 数据格式合规性 | `X=A/B`；按标准原文，A 为满足业务精度需求的元素数，B 为被评价元素数 | 需预期类型、范围、长度及精度 |
| 030300 | `db31_030300` | 二级 | 数据重复率 | `X=1-A/B`；A 为重复元素数，B 为被评价元素数 | **可直接计算** |
| 030400 | `db31_030400` | 二级 | 数据唯一性 | `X=A/B`；A 为满足唯一性要求的元素数，B 为被评价元素数 | **可直接计算** |
| 030500 | `db31_030500` | 二级 | 脏数据出现率 | `X=1-A/B`；A 为脏数据元素数，B 为被评价元素数 | 需可执行的脏数据定义 |
| 030600 | `db31_030600` | 二级 | 数据标准参照准确性 | `X=A/B`；A 为与明确权威参照一致的元素数，B 为被评价元素数 | 需权威参照和匹配方式 |

### 3.4 公共数据一致性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 040100 | `db31_040100` | 二级 | 相同数据一致性 | `X=A/B`；A 为满足数据源一致性的数据数，B 为被评价元素数 | 需其他位置、应用、用户或版本中的同一数据 |
| 040200 | `db31_040200` | 二级 | 关联数据一致性 | `X=(A+B+C+D)/4`；A 至 D 分别为 040201 至 040204 得分 | 需四类关联约束的完整得分 |
| 040201 | `db31_040201` | 三级 | 表内等值一致性 | `X=A/B`；A 为满足表内跨列等值一致性的数据数，B 为被评价元素数 | 需字段对、条件和空值语义 |
| 040202 | `db31_040202` | 三级 | 表内逻辑一致性 | `X=A/B`；A 为满足表内跨列逻辑一致性的数据数，B 为被评价元素数 | 需跨字段逻辑规则 |
| 040203 | `db31_040203` | 三级 | 跨表等值一致性 | `X=A/B`；A 为满足跨表等值一致性的数据数，B 为被评价元素数 | 需关联表、连接键和等值约束 |
| 040204 | `db31_040204` | 三级 | 跨表逻辑一致性 | `X=A/B`；A 为满足跨表逻辑一致性的数据数，B 为被评价元素数 | 需关联表、连接键和逻辑规则 |
| 040300 | `db31_040300` | 二级 | 内容数据记录数据项与元数据一致性 | `X=A/B`；A 为内容数据项与元数据一致的数据数，B 为被评价元素数 | 需独立元数据定义 |

### 3.5 公共数据时效性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 050100 | `db31_050100` | 二级 | 基于时间段的正确性 | `X=A/B`；A 为满足有效性要求的元素数，B 为被评价元素数 | 需有效日期范围、预期记录数或频率分布 |
| 050200 | `db31_050200` | 二级 | 基于时间点的及时性 | `X=A/B`；A 为满足及时性要求的元素数，B 为被评价元素数 | 需公开时间、截止时间、更新频率或有效期 |
| 050300 | `db31_050300` | 二级 | 时序性 | `X=A/B`；A 为满足时序性要求的元素数，B 为被评价元素数 | 需实体键、时间字段和先后规则 |
| 050400 | `db31_050400` | 二级 | 数据授权使用时效性 | `X=A/B`；A 为满足授权使用时效性要求的元素数，B 为被评价元素数 | 需授权周期、对象和当前使用上下文 |

### 3.6 公共数据可访问性

| 代码 | 稳定 ID | 层级 | 指标 | 计算方式 | v0.6 当前能力 |
|---|---|---|---|---|---|
| 060100 | `db31_060100` | 二级 | 可访问 | `X=A/B`；A 为满足可访问性要求的元素数，B 为被评价元素数 | 需访问服务、需要时间和访问 SLA |
| 060200 | `db31_060200` | 二级 | 可用性 | `X=A/B`；A 为满足可用性要求的元素数，B 为被评价元素数 | 需目标应用场景和验收标准 |

## 4. v0.6 对 030300 和 030400 的可执行口径

当前 Demo 对单个上传表采用“内容记录、精确相等”口径：

- `N`：记录总数；
- 比较字段：排除明显技术标识字段后的内容字段；若全是标识字段则回退到全部字段；
- `D`：每个精确重复组除首次记录外的后续重复记录数之和；
- `U = N - D`。

计算如下：

```text
exact_duplicate_rate = D / N       # 原 v0.4 指标，越低越好
db31_030300 = 1 - D / N            # DB31/T 质量得分，越高越好
db31_030400 = U / N = (N - D) / N  # DB31/T 质量得分，越高越好
```

三个指标可被同时选择，分别使用不同 `metric_key`。规范化后才相同但原始内容不完全相同的记录不计入两个 DB31/T 得分。

## 5. “无法评估”与代理指标

其余 28 项会保留在报告中，并输出：

- 稳定的 `reason_code`；
- `required_inputs`，即完成标准评价所需的外部依据；
- `available_proxy_metric_ids`，即当前原有指标中可供排查的线索；
- `proxy.standard_equivalent = false`，明确代理结果不等于标准得分。

例如，字段内部类型一致率可以提示混合类型，但不能证明字段符合外部数据类型标准；文件可解析也不能证明数据在需要时间满足访问 SLA。因此 v0.6 不会把这些代理值标记为 DB31/T 的“已评估”结果。

## 6. 用户选择与可复现性

- 页面默认选择原 v0.4 的 13 项，也可一键选择仅 DB31/T 或全部 43 项。
- 自由选择允许原有指标和标准指标任意混合。
- 空选择不能运行。
- 选择集合按目录顺序规范化；点击顺序不同不会改变报告顺序或哈希。
- `QualityReport 0.3` 的 `evaluation_context` 记录 `metric_catalog_version` 和 `selected_metric_ids`。
- RulePack 重评必须重建完全相同的选择集合；选择不一致会因报告哈希不匹配而拒绝执行。
