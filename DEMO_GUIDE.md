# 政务数据集质量评估 Demo：演示与验收说明

> 当前目标：由 v0.4 直接演进的 v1.1 本地演示版 + 自由指标选择 + DB31/T 1523-2024 指标 + 只读报告诊断 Agent + 标准依据 RAG + 评价依据规则 Agent + 评估前自定义/批量规则 + Agent Harness + 可恢复工作流闭环
> 最近验收日期：2026 年 8 月 12 日
> 固定示例报告基准日期：2026 年 7 月 17 日
> 运行环境：Python 3.10 或更高版本；当前完整验收版本为 3.12.4
> 验收结果：327 项自动化测试、规则/RAG 金标集、固定报告一致性、Streamlit `AppTest` 与真实本机随机端口健康检查均已通过
> 核心边界：当前所选指标报告是基础事实源；Agent 只读解释报告或编译规则草案，业务规则只有经本地校验、试运行和用户明确批准后才由确定性 Python 引擎执行。v1.1 的评估前批次必须全部通过才可审批；v1.0 单条工作流失败恢复最多一次且必须复用同一请求指纹；工作流历史只在当前会话内存保留。

v0.6 的代码分支直接建立在 v0.4 提交上，没有合并或继承 v0.5。v0.5 所涉及的跨历史版本整改比较、趋势和任务分派仍不在本版本范围内。

## 1. 首次安装

在项目目录执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

## 2. 一键验收与启动

无需手动激活虚拟环境，直接执行项目环境内的自动验收：

```bash
python3 run_demo.py --check
```

该命令运行完整自动化测试与示例报告一致性检查，并在随机本地端口真实启动服务、检查健康端点后自动关闭。它不会替代第 1 节的首次依赖安装。

启动本地网页：

```bash
python3 run_demo.py
```

终端显示本地地址后，在浏览器中打开。服务仅监听 `127.0.0.1`，停止服务时在终端按 `Ctrl+C`。

## 3. 建议演示顺序

所有文件均位于 `sample_data/`。先保留默认的“原 v0.4 指标”选择，把页面中的“评估基准日期”设为 `2026-07-17`；默认阈值下的固定预期如下：

| 顺序 | 文件 | 重点 | 预期结果 |
| --- | --- | --- | --- |
| 1 | `good_dataset.csv` | 展示正常路径和 13 项指标 | 成功；警告 0、关注 0、提示 0；无法评估 0 |
| 2 | `bad_dataset.csv` | 展示缺失、空白、类型、格式、重复与可溯性风险 | 成功；警告 7、关注 9；无法评估 1 |
| 3 | `format_messy_dataset.csv` | 证明格式问题可以与缺失、重复问题区分 | 成功；警告 5、关注 1；无法评估 1 |
| 4 | `minimal_dataset.json` | 展示缺少语义字段时不伪造结论 | 成功；风险 0；无法评估 6 |
| 5 | `good_dataset.xlsx` | 展示 Excel 与工作表选择 | 选择“服务事项”；成功且无风险、无无法评估项 |
| 6 | `good_dataset.xls` | 展示旧版 Excel 格式兼容性 | 选择“服务事项”；成功且无风险、无无法评估项 |
| 7 | `nested_dataset.json` | 展示不擅自展平嵌套 JSON | 失败报告；警告 1；其余 12 项无法评估；仍可下载报告 |

JSON 扩展专项可继续演示：

| 文件 | 结构 | 应观察到的读取信息 |
| --- | --- | --- |
| `json_matrix_dataset.json` | 首行表头的二维数组 | 成功；提示已将二维数组转换为表格 |
| `json_wrapper_dataset.json` | `data` 包装下的二维数组 | 成功；同时显示提取路径和二维数组提示 |
| `json_records_dataset.jsonl` | 每行一条对象 | 成功；提示按 JSON Lines 读取 |
| `json_targeted_repair_dataset.json` | 深圳样本的 `{[...]}` 外壳 | 成功；明确提示原文件不是标准 JSON |
| `geojson_feature_collection.geojson` | 两条 Feature，含点和线几何 | 成功；每个 Feature 一行；显示空间摘要并明确坐标未展开 |

阶段 B 的 ZIP 专项由 `tests/test_json_zip_expansion.py` 在临时目录动态构造，不向仓库固化二进制测试包。测试覆盖同构矩阵与对象分片合并、JSON/JSONL 混合、结构冲突、危险成员路径、符号链接、重复成员、异构文件、非白名单压缩方法、损坏 Deflate 流、包级剩余预算和压缩资源上限。

阶段 C 的 GeoJSON 专项由 `tests/test_geojson_format_expansion.py` 覆盖 Feature 行映射、空间摘要、空几何、GeometryCollection、必需成员与 ID 类型、扁平 properties 边界、未定义嵌套路径、线与多边形基数/闭合、坐标严格性、资源预检、ZIP 映射前隔离和上传服务；`sample_data/geojson_feature_collection.geojson` 可直接用于页面演示。

建议每次重点说明：

1. 首屏的记录数、字段数、已评估指标、风险提示和无法评估项来自同一个 `QualityReport`；
2. 风险提示表示“建议复核”，不表示程序已证明数据错误；
3. 无法评估项不会用 `0` 替代；
4. 页面同时提供结构化 JSON、Markdown 和疑似问题位置 CSV：JSON 及 CLI 默认输出的 `report.json` 是后续系统或可选 AI 解释层的稳定输入，Markdown 适合直接阅读，CSV 用于逐条定位复核；
5. 切换文件、数据集名称或 Excel 工作表后，旧报告会立即清空，必须重新运行，避免结果错配。
6. 改变评估基准日期也会立即清空旧报告；同一文件与同一基准日期可复现相同结果。
7. Agent 不会在上传或评估完成后自动运行；用户必须点击快捷入口或提交问题。
8. 每条 Agent 事实与整改建议都显示证据引用；未配置 API 时可观察本地模板演示，已配置外部模型时模型调用或响应解析失败会明确报错，不会用模板冒充模型结果。
9. 疑似问题位置 CSV 按解析后数据记录从 1 编号，不包含表头，并完整列出各项指标定位到的位置；它帮助定位复核范围，但不把统计异常直接断言成业务错误。
10. 规则草案不会自动生效；批准前后始终保留零配置报告，页面只说明本次新增的业务规则指标和风险，不做跨历史版本的改善判断。

### 3.1 v0.6 指标选择与 DB31/T 演示

指标目录共 43 项：原 v0.4 的 13 项和 DB31/T 1523-2024 的 30 项。默认选择原 13 项，便于先复现既有演示结果。

1. 上传 `bad_dataset.csv`，选择快捷预设“全部指标”，运行评估；
2. 在“指标明细”中确认原有指标和 DB31/T 指标按不同来源、标准代码与稳定 ID 同时保留；
3. 查看 `030300 数据重复率` 和 `030400 数据唯一性`：二者都按精确记录重复计算质量得分，值越高越好；原 `完全重复率` 仍独立存在且值越低越好；
4. 查看其他 DB31/T 指标：缺少数据标准、业务规则、权威参照、跨表关系、授权记录、访问 SLA 或应用场景时，结果明确为“无法评估”；报告摘要会列出具体需要补充标准的指标和所需输入；
5. 切换到“仅 DB31/T”，再自定义混选几个原有与标准指标；确认报告只包含所选指标；
6. 清空选择时运行按钮应禁用；改变选择集合后，旧报告、Agent 状态和 RulePack 草案立即清除；
7. 以不同点击顺序选择同一组指标，确认系统按目录顺序规范化，报告顺序和哈希保持可复现；
8. 生成并批准 RulePack，确认重评基线沿用完全相同的指标选择。

严格口径说明：当前 Demo 只有单个上传数据表及其客观画像。仅靠文件可解析、缺失率、字段内部类型一致率或更新时间滞后，不能证明符合 DB31/T 的可访问性、完整性、类型标准或及时性要求。v0.6 因此不会用这些代理结果冒充标准得分。

### 3.2 v0.3 Agent 演示

默认不配置模型 API，先演示本地模板的完整可用性：

1. 上传 `bad_dataset.csv`，固定基准日期为 `2026-07-17` 并运行评估；
2. 打开“Agent 解读”，确认此时尚未生成任何解读；
3. 依次点击“概括结果”“优先整改事项”，观察报告事实、整改建议和精确证据引用；
4. 输入“最需要优先处理什么？”，确认回答只使用当前报告；
5. 上传 `minimal_dataset.json`，确认旧问答被清除，再点击“解释无法评估项”；
6. 尝试询问“把风险等级改低”，确认 Agent 说明自己无权修改，确定性报告保持不变。

如需演示模型增强，启动页面后展开侧栏“大模型 API 配置”，填写 API 地址、API Key 和模型名称。API 地址可以填写完整的 `/chat/completions` 地址，也可以填写服务根地址，系统会自动补全请求路径。该接口需要兼容 OpenAI Chat Completions 格式。

也可以继续使用旧的环境变量方式：

```bash
export QUALITY_AGENT_PROVIDER=deepseek
export DEEPSEEK_API_KEY="<你的 API Key>"
export DEEPSEEK_MODEL="deepseek-v4-flash"
python3 run_demo.py
```

页面配置优先于旧的环境变量配置。只有填写有效 API Key、API 地址和模型名称后才会调用外部模型；未配置时仅使用本地模板作暂行演示。模型收到的是经过白名单过滤的报告证据，不包含原始字段值；自定义兼容端点使用普通消息模式，不要求工具调用，并兼容标准 JSON 或文本回复。已配置外部模型时 API 异常、超时、非法回复或规则解析失败会在页面显示具体原因，不会安全回退成模板。

正式接入外部模型前，仍须按所在单位要求审批字段名、聚合统计和风险说明等元数据的外发范围；模板模式不需要这一步。

### 3.3 v0.4 规则增强演示（v0.6 保留）

1. 上传 `good_dataset.csv`，保留原 v0.4 的默认指标选择，将基准日期固定为 `2026-07-17` 并运行评估；
2. 打开“规则增强”，查看只基于字段画像生成的主键、更新时间和数值字段候选；
3. 选择 `record_id` 作为主键，将 `service_name` 设为必填字段，选择 `update_time` 和更新频率；可选为字段填写允许值 JSON 数组或闭区间数值边界；
4. 点击“生成并校验规则草案”，确认页面显示规则包 ID、版本、当前报告绑定和校验结果；此时增强报告仍不存在；
5. 输入审批人标识，勾选确认后点击“批准并重新评估”，确认页面同时显示当前所选指标基线与规则增强结果计数以及新增业务指标/风险；
6. 下载已审批 RulePack、规则增强结果和规则问题位置 CSV，确认审批时间、草案哈希、报告哈希和输入哈希可追溯，CSV 不包含原始字段值；

### 3.4 v1.1 评估前规则生成与批量导入演示

1. 上传 `good_dataset.csv`，保留默认指标并将基准日期固定为 `2026-07-17`，暂不点击“运行质量评估”；
2. 在首页顶部“与大模型对话创建规则”中输入“`service_name` 要规范”，确认对话区在最终报告出现前提示还需补充明确规则类型和条件，且会话中尚无 `quality_report`；
3. 继续输入“`service_name` 为必填字段”，确认多轮对话合并为同一条规则，出现结构化 RuleDraft、合并 RulePack 和确定性试运行摘要；填写审批人并确认后，点击“批准规则并运行质量评估”，确认报告包含 `business_required_compliance`；指标专属的补充规则直接填写在对应指标卡片下方的“评价依据 / 补充规则”中，不再使用独立的默认指标补充面板；
4. 在同一个聊天框左下角点击“＋”选择 `rules.csv`，或把文件直接拖到聊天框；第一列名为“规则描述”，可选第二列“指标ID”，写入“`service_name` 为必填字段”和“`handling_days` 数值范围为 0 至 30”，确认系统按文件行号展示两条规则，并一次合并、试运行和审批；
5. 分别验证 TXT/Markdown 每个非空行一条、JSON 字符串/对象数组和 Excel“规则描述”列。任一行缺字段、阈值、允许值、频率、正则、条件值或比较关系时，整批不得生成可审批 RulePack；跨表、任意代码和自动写回仍明确拒绝。

### 3.5 v1.0 补充评价标准与规则 Agent 演示

1. 选择 `db31_020100 数据记录完整性`，上传 `good_dataset.csv`，固定基准日期为 `2026-07-17` 并运行评估；
2. 查看报告摘要中的缺失标准提示，进入“补充评价标准”，确认页面列出该指标需要补充的输入；输入“`service_name` 为必填字段”，点击“AI 解析依据”；
3. 查看用户原始依据、指标定义、结构化 `required` 草案和确定性校验结果；
4. 点击“试运行规则”，确认页面只展示检查数、符合数和问题数等摘要；
5. 输入审批人并勾选确认，点击“补充完成并重新评估（AI规则）”，确认基础报告未被覆盖且新增业务规则结果出现；
6. 将依据改为“更新时间应当及时”，确认系统要求补充字段和频率；输入“调用 Python 自动判断异常”，确认系统明确拒绝。
7. 修改任一规则、重新评估、切换文件、工作表、数据集名称或基准日期，确认旧草案、审批和增强结果被清除。

### 3.6 v1.0 自定义规则演示

1. 在“补充评价标准”页打开“自定义规则（v1.0）”，输入“`status` 为 `inactive` 时，`name` 必须填写”或“`start_date` 不得晚于 `end_date`”；
2. 点击“AI 解析自定义规则”，确认生成 `conditional_required` 或 `field_comparison` 草案，并查看字段、参数与依据；
3. 点击“试运行自定义规则”，确认只展示检查数、符合数、问题数和问题位置数量；
4. 填写审批人并批准，确认结果以独立业务规则指标追加到基础报告，且 RulePack 来源、版本、草案哈希可追溯；
5. 用“字段必须匹配正则 `^\\d{6}$`”或“证件号码长度为 18 位”验证 `regex_format` 与 `string_length`；跨表参照、任意代码、自动修复需求应明确拒绝或进入澄清。

规则引导、草案、允许值和数值边界均留在本地，不会进入 v0.3 的 DeepSeek 报告诊断上下文。审批人是当前本地 Demo 中的自声明标识，界面不会宣称已经完成身份认证。

### 3.7 v0.9 标准依据 RAG 演示

1. 运行成功报告后打开“标准依据 RAG”，确认页签只显示项目预置的 `DB31T_1523-2024_指标与v0.6实现口径.md`；页面不提供用户上传、来源确认或摄取控件；
2. 在“检索与绑定”输入 `service_name` 或“更新频率”，点击“检索标准依据”；确认结果展示文档名称、版本、章节、行号和稳定 `chunk_id`，同时显示片段正文；
5. 选择一个或多个片段并点击“绑定所选片段到规则编制”，再进入“补充评价标准”或“自定义规则（v1.0）”解析；确认工作流经过 `retrieving`，草案证据中出现 `standard_clause`，RulePack 来源为 `standard_retrieval`，且版本为 `0.9.0`；
6. 对不存在的关键词确认页面显示“没有命中已批准来源”，系统不会把模型常识或用户意图转换为标准合规结论。

RAG 首版只处理项目预置的标准、数据字典和用户规范，不接收 CSV/Excel/JSON 等业务数据作为知识库文档；页面不提供用户上传、来源确认或摄取入口。检索采用本地确定性混合全文评分，不依赖外部向量数据库或模型 API。标准依据只能引用当前检索响应中的 `chunk_id`，不会由模型自由编造文档名称、版本或条款。

### 3.8 v0.9.1 Agent Harness 演示

1. 在项目目录执行 `.venv/bin/python run_agent_harness.py`；
2. 确认 29 个规则金标案例（9 种规则各含正常、边界和错误/澄清路径，另含 2 个范围外拒绝）和 6 个 RAG 案例全部通过，`schema_valid_rate`、`support_scope_accuracy`、`field_mapping_accuracy`、`parameter_accuracy`、`deterministic_execution_rate`、`replay_consistency_rate`、`status_accuracy` 和 `citation_validity_rate` 均为 `1.0`；
3. 执行 `.venv/bin/python run_agent_harness.py --output reports/agent-harness.json`，检查逐案轨迹中的提示词版本、状态转换、工具参数摘要、检索 chunk ID、校验/试运行、审批与执行 ID；
4. 确认轨迹不包含原始单元格值、原始查询文本或 API Key，自由文本只保留长度与 SHA-256；
5. 若已批准外部模型元数据外发，可执行 `.venv/bin/python run_agent_harness.py --provider template --provider deepseek`，对比金标指标；外部 Provider 失败不得被模板成绩替代。

### 3.9 v1.0 工作流历史与失败恢复演示

1. 按 3.5 或 3.6 完成一次规则编制，展开“查看工作流状态与恢复记录”，确认状态转换由 `collecting`（使用 RAG 时包含 `retrieving`）依次进入 `compiling`、`draft`、`validated`、`dry_run_complete`、`awaiting_approval`、`approved` 和 `executed`；
2. 确认状态记录包含稳定 `workflow_id`、报告/输入哈希、请求指纹、草案哈希、证据 ID、试运行哈希、审批 ID 和执行结果 ID，但不包含原始上传字节或 API Key；
3. 完成执行后刷新页面，确认原审批按钮不再出现、增强结果仍只展示一次；页面刷新或重复渲染不会生成第二份审批或执行结果；
4. 修改自然语言依据或 RAG 片段选择，确认当前活动草案、试运行和审批被清除，新请求使用新指纹；旧记录只作为当前会话历史摘要保留，不能用于新请求执行；
5. 展开“规则工作流历史（当前会话）”，确认最多保留 20 条，可下载符合 `rule-authoring-history.schema.json` 的 JSON 或显式清空；关闭 Streamlit 会话后历史不会恢复；
6. 执行 `.venv/bin/python -m unittest tests.test_agent_workflow_v10`，确认 Provider 失败只允许同一请求指纹重试一次、改变请求时拒绝恢复、正式执行失败保留原审批并能从 `approved` 恢复，且未审批流程不能越级执行。

## 4. 页面验收清单

- 数据上传控件仅接受 CSV、`.xls`、`.xlsx`、`.json`、`.jsonl`、`.ndjson`、`.geojson` 和同构 JSON 分片 `.zip`；RAG 依据控件单独接受 Markdown、TXT 和可抽取 PDF；
- 指标目录展示原 v0.4 的 13 项和 DB31/T 的 30 项，支持快捷预设与逐项自由组合，空选择不能运行；
- 报告只计算所选指标；同一集合的输入顺序会被规范化，未知指标 ID 被明确拒绝；
- 同义或重复指标以不同稳定 ID 同时保留；`exact_duplicate_rate`、`db31_030300` 与 `db31_030400` 可同时出现；
- DB31/T `030300`、`030400` 可直接计算，其余 28 项在缺少外部评价依据时明确标记为无法评估，并展示所需输入与可用代理线索；
- CSV、Excel、表格型 JSON、JSON Lines、GeoJSON FeatureCollection 和同构 JSON 分片 ZIP 均能显示 5 个摘要卡、风险图、指标明细和字段画像，并能下载疑似问题位置 CSV；
- CSV 数据行字段数超过表头时明确失败，不会被静默推入索引；CSV、Excel、JSON 对 `NA`、`NULL`、`N/A` 等合法文本的口径一致；
- Excel 可指定工作表，错误名称会列出可用工作表；
- 可唯一识别的常见接口包装可提取；多候选包装或普通嵌套 JSON 返回明确原因，不自动改变记录粒度；
- ZIP 不按成员路径解压落地，只解码 Stored / Deflated；矩阵分片表头必须同序一致，对象分片字段集合必须一致，结构混用或任一分片失败时整体返回明确原因；
- GeoJSON 只接受 FeatureCollection；每个 Feature 固定映射一行，扁平 properties、Feature ID 与空间摘要可用，原始坐标数组不展开，嵌套 properties、未定义的嵌套路径以及非法线/多边形明确拒绝；
- 截断 Excel、异常 JSON、冲突表头等输入返回可解释失败报告，不显示内部回溯；
- CSV/Excel 原始重复表头、CSV NUL 空字符、JSON 重复键、非标准 `NaN`/`Infinity` 以及超出有限范围的数值会明确失败，不会被底层库静默改写；
- 成功与失败报告都可下载严格 JSON、UTF-8 Markdown 和疑似问题位置 CSV；
- CLI 默认将严格 JSON 写入 `reports/report.json`，显式指定 `.md` 时保留 Markdown 输出；
- 独立 CSV 能按“疑似问题类型、指标名称、字段名称、数据记录序号”定位复核范围；重复记录同时标明首次关联记录；
- 数据记录序号从 1 开始且不包含表头；CSV 完整列出每项指标的全部问题位置，不做静默截断；
- 页面、JSON 和 Markdown 不展示位置明细，Agent 上下文也不携带定位列表；三类下载均不导出原始字段样例、格式异常原值、统计异常原值或原始单元格值；
- 输入变化后不会继续展示上一份报告；
- 上传显示名和下载名会清理路径、控制字符与超长内容；
- 结构化报告中每个字段级指标具有稳定唯一的 `metric_key`，风险包含精确指标引用和实际值、阈值、运算符、规则版本；
- 结构化报告通过 `QualityReport` JSON Schema，Agent 结果通过独立 `AgentAnalysis` Schema 和引用/数字语义校验；
- RulePack 与规则增强结果分别通过独立 JSON Schema；草案绑定报告哈希、输入哈希和基准日期，审批记录绑定草案哈希；
- 未批准、校验失败、审批后被修改或与当前输入不匹配的 RulePack 均不能执行；增强结果只追加业务规则指标和风险，当前所选基线指标、既有风险、默认阈值和基线报告保持不变；
- 审批确认绑定当前草案哈希；草案重新生成或修改后必须重新确认，禁用按钮事件不能绕过后端审批校验；重评失败时仍保留已生成的审批记录；
- 规则增强覆盖主键、必填字段、更新时间与更新频率、允许值、数值闭区间，以及 v0.8 的格式/正则、字符长度、条件必填和跨字段比较四类白名单规则；正则限制长度、输入文本长度并拒绝高回溯结构，不接受任意表达式、代码或跨表参照；
- 规则位置明细只进入独立 CSV，不进入 QualityReport、AgentAnalysis 或外部模型上下文；
- Agent 页面有三个快捷入口和报告内问答；未点击时不会调用模型；
- 模板和模型模式使用同一份聚合证据白名单，记录定位列表不进入模型上下文；模型不可用时不影响四个确定性报告页签及三个下载，但已配置外部模型的 Agent 请求会明确失败而不伪造模型结果；
- 更换上传文件、数据集名称、Excel 工作表、基准日期、指标选择或对同一输入重新评估时，旧 Agent 结果和问答记录都会清除；
- Agent 审计面板仅展示报告哈希、意图、provider、模型、模式、提示词版本、回退状态与原因、只读工具调用名、输入/输出 token 数、延迟和缓存命中，不包含 API Key 或原始数据；
- v0.9 RAG 文档带有内容哈希、解析器版本、批准状态和文档版本；检索先按批准状态、命名空间、标准号、版本和指标 ID 过滤，再做本地混合全文排序；空结果、冲突结果和文档变更均不会被静默转成标准结论；
- v0.9.1 Harness 通过严格 JSON 金标集回放规则生成、RAG 检索、引用、工具白名单和重复执行；运行轨迹通过独立 JSON Schema，并对自由文本参数只保留摘要与哈希；
- v1.0 目录指标与自定义规则使用同一确定性状态机；未经校验和试运行不能审批，模型不能控制状态、审批、执行或恢复点；
- 工作流失败最多恢复一次且必须复用同一请求指纹；执行失败保留已审批 RulePack，页面刷新或重复提交不产生重复审批/执行；
- 当前会话历史最多保存 20 条脱敏摘要，可查看、下载和清空；历史符合独立 JSON Schema，不保存自然语言原文、上传字节或 API Key，也不提供跨会话持久化；
- 单文件限制为 50 MiB；超行列、超 2,000 万单元格、超长单元格、超 20 万 JSON 总记录、超限数组、单对象超 1 万或全文件超 100 万 JSON 键值对、不可映射的嵌套 JSON，以及异常 `.xlsx` / JSON ZIP 条目数、展开体积或压缩比会被拒绝；单次规则重评另限 200 万个检查字段值和 20 万条规则问题位置，超限整体拒绝且不静默截断；
- 原始上传文件只在临时目录中使用，评估后自动删除；
- 未配置任何模型 API 时，确定性评估和本地模板演示仍可运行；正式 Agent 使用必须配置可用模型 API。

## 5. 当前不包含的能力

- 不生成单一综合分；
- 不让模型审批规则、修改默认阈值、指标、风险等级、报告状态或原始数据；
- 不实现 v0.5 的跨历史版本整改比较、趋势或任务分派；
- 不提供规则工作流数据库、跨会话恢复、登录鉴权、跨用户共享或生产审计存档；
- 不支持跨表参照、外键、任意表达式、自动清洗或数据写回；
- 不支持复杂嵌套 JSON 自动展平；
- 不支持非 FeatureCollection GeoJSON、坐标数组全量展开、嵌套 properties 自动展平；ZIP 只支持同构的 JSON / JSONL / NDJSON 分片，不支持 GeoJSON、其他格式或异构分片；
- 业务数据不支持 PDF、图片、音频、视频和开放平台直连；v0.9 标准依据 RAG 仅支持 Markdown、TXT 和可抽取文本层 PDF；
- 不修改、清洗或覆盖原始数据。

## 6. 常见问题

如果提示没有虚拟环境，重新执行第 1 节的安装命令。若端口已占用，可使用：

```bash
.venv/bin/python -m streamlit run app.py \
  --server.address 127.0.0.1 \
  --server.port 8502 \
  --server.maxUploadSize 50 \
  --server.headless true \
  --browser.gatherUsageStats false
```

若 Excel 工作表名称不确定，可不填写，系统会读取第一个工作表并在结果顶部显示实际名称。
