# 政务数据集质量评估 Demo：开发计划表

> 版本：v1.2（规则生成协议、规则卡片目标映射、API Key 本机凭据保存和页面文案收敛已完成；当前进入专项测试加固；保留 v0.1-v1.1 历史排期与验收记录）
> 计划区间：v0.1 原计划为 2026 年 7 月 15 日 - 8 月 28 日；v0.6 于 2026 年 8 月 3 日完成本地开发、验收与 GitHub 推送；v0.7-v1.0 Agent 路线于 2026 年 8 月 6 日批准；v0.9 于 2026 年 8 月 9 日完成本地开发与验收；v0.9.1、v1.0 与 v1.1 于 2026 年 8 月 10 日完成开发、验收、本地提交和 GitHub 独立分支上传。v1.1 提交为 `2cda777`，草稿 PR #9 为 `codex/v1.1 → codex/v1.0`。
> 目标：以确定性质量评估引擎为事实基础，在最终评估前完成默认指标补充规则、自然语言自定义规则或规则文件批量输入的完整性检查、草案生成、确定性校验、统一试运行与人工审批，再执行最终评估。
> 最近进度更新：2026 年 8 月 14 日。
> 排期说明：v0.6 直接基于 v0.4 开发，不合并 v0.5 的历史比较与整改闭环。v0.7 已完成评价依据 Agent；v0.8 已完成自然语言自定义规则；v0.9 已完成标准依据 RAG；v0.9.1 已完成 Agent Harness；v1.0 已完成显式工作流、失败恢复、会话历史和页面闭环；v1.1 已将规则生成和澄清前移到最终评估之前，并支持规则文件整批处理。外部模型模式仍不模板回退，规则失败会展示澄清或拒绝原因。

## 1. 本期目标与边界

| 项目 | 说明 |
| --- | --- |
| 本期硬交付 | 在原有 43 项指标评估、自然语言规则和标准依据 RAG 基础上，允许用户为默认指标补充规则、在首页与大模型多轮对话创建自定义规则或导入规则文件；描述不完整时必须在最终评估前逐条澄清，全部规则通过后才可统一试运行、审批和执行。 |
| Agent 定位 | v1.1 的模型只生成候选规则草案；输入完整性门禁、支持范围、文件解析、Schema 校验、试运行、审批、正式执行、幂等与恢复均由本地确定性代码控制。没有模型 API 时，基础评估、标准检索和模板演示仍可用。 |
| 已批准路线完成情况 | v0.7 指标评价依据 Agent、v0.8 自然语言自定义规则、v0.9 标准依据 RAG、v0.9.1 Agent Harness、v1.0 可演示 Agent 闭环和 v1.1 评估前规则批量生成均已完成本地开发与验收。 |
| 不作为硬交付 | 不做 PPT；不做大规模模型微调或因果性模型效果验证；不支持扫描件/图片型 PDF、图片、音频及直接连接开放平台；不承诺跨会话知识库或自动标准符合性判定。 |

**关键策略：** 不依赖大模型的评估引擎、标准检索和网页闭环始终作为基础能力；v0.3 Agent 只读解释确定性报告，v0.4 规则闭环由本地受控服务执行。v0.6 只扩展指标目录和选择/展示流程，不包含 v0.5 历史、比较和整改任务。v0.7 起模型可以参与评价依据理解和规则草案编制，v0.9 RAG 可以提供可定位的标准证据；v1.1 在模型调用前后均设置确定性完整性门禁，任何一条规则待澄清或失败时整批不进入最终评估。模型始终不直接计算指标、批准规则或执行规则。

## 2. 开发时间表

| 日期 | 阶段 | 本周工作 | 产出 | 验收标准 |
| --- | --- | --- | --- | --- |
| 7/15 - 7/17 | 阶段 0：立项与骨架 | 确定输入/输出边界；创建 Python 项目目录；定义 `report.json` 结果结构；准备 3 类测试数据。 | 项目可启动；输入输出协议；测试数据清单。 | 能在本机启动项目；明确仅支持 CSV、Excel、JSON。 |
| 7/20 - 7/24 | 阶段 1：文件解析与画像 | 实现 CSV、Excel、JSON 读取；处理编码、工作表与读取失败；生成行数、字段数、字段名、样例、空值与基础类型画像。 | 统一数据表对象；数据画像模块。 | 三类文件均可读取，失败时返回可解释错误。 |
| 7/27 - 7/31 | 阶段 2：核心指标引擎 | 实现 13 项零配置指标：缺失、空白记录、类型一致、格式异常、完全/规范化重复、时间、来源、版本、IQR 异常等。 | 指标函数库；结构化指标报告。 | 同一输入重复运行结果一致；每项指标有数值或明确的无法计算原因。 |
| 8/3 - 8/7 | 阶段 3：规则与验证 | 实现“已评估 / 风险提示 / 无法评估”三层结果；为好、差、格式混乱的数据集编写自动测试；整理默认阈值与配置入口。 | 风险规则模块；回归测试；示例报告。 | 能区分三类质量表现；不把缺少业务规则的问题误判为错误。 |
| 8/10 - 8/14 | 阶段 4：界面与导出 | 用 Streamlit 实现上传、运行、结果总览、指标明细、风险提示和无法评估项；支持下载 JSON（可选 Excel）。 | 本地网页 Demo；报告下载。 | 从上传到查看结果可在一个页面完成；异常文件有清晰提示。 |
| 8/17 - 8/21 | 阶段 5：可选 Agent 增强 | 仅在确有展示需求且模型方案获批后，接入字段语义辅助与报告解释；只读消费结构化报告，模板继续作为默认说明。 | 可选解释器；模板回退模式。 | 无 API 时完整流程不受影响；有 API 时只补充解释，不改变指标、风险与无法评估项。 |
| 8/24 - 8/28 | 阶段 6：联调与交付 | 以至少 3 个样例做端到端测试；修复阻塞问题；补齐 README、安装步骤和一键启动命令；冻结可演示版本。 | 可运行代码包；README；最终示例结果。 | 在新环境按 README 可启动；完整演示流程稳定通过。 |

### 2.1 当前进度快照（2026-08-14）

| 阶段 | 状态 | 已完成 | 未完成 / 下一步 |
| --- | --- | --- | --- |
| 阶段 0：立项与骨架 | 已完成 | 已形成输入输出协议、13 项零配置指标目录、Python 项目骨架和 3 类测试数据。 | 无。 |
| 阶段 1：文件解析与画像 | JSON 扩展 A / B / C 已完成（代码复审后） | 在既有形态上补齐大整数保真、整棵 JSON Unicode 严格校验、JSONL 行号/空白边界、Stored / Deflated ZIP 白名单、损坏压缩流异常收口、包级剩余资源预算、GeoJSON 必需成员、嵌套路径、线/多边形合法性与 ZIP 映射前隔离。 | 后续仅在有明确规则和资源边界时考虑 GeoJSON ZIP、坐标全量展开或更多白名单路径；普通嵌套、异构 ZIP 和 JSON5 仍明确拒绝。 |
| 阶段 2：核心指标引擎 | v0.6 已完成（43 项可选） | 保留 13 项基础指标，并加入 DB31/T 1523-2024 的 30 项独立定义；选择集合按稳定 ID 和目录顺序固化。`030300`、`030400` 可直接计算，其余缺少外部依据的标准指标明确输出 `not_assessable`。 | 后续新增指标须先补齐定义、计算口径、评价依据、报告契约与回归测试；不得以代理值冒充标准得分。 |
| 阶段 3：规则与验证 | 已完成 | 已实现 `info` / `attention` / `warning` 三档风险提示、集中阈值配置和三层报告；`not_assessable` 不触发风险，IQR 只生成 `info`。 | 后续根据真实数据调整集中阈值，不需改动指标计算函数。 |
| 阶段 4：界面与导出 | 已完成（安全复审后） | 已实现 Streamlit 完整页面；上传限制为 50 MiB；页面同时提供严格 JSON、UTF-8 Markdown 和完整疑似问题位置 CSV，CLI 默认输出 `report.json`；位置明细不进入页面表格、JSON、Markdown 或 Agent，只通过独立 CSV 下载，且不包含原始字段或异常样例；上传与下载文件名同时受字符数与 UTF-8 字节数限制；服务只监听本机。 | 后续只做联调修复，可选 Excel 下载不属于当前硬交付。 |
| 运行环境、测试与文档 | v1.2 基线已完成，专项加固进行中 | v1.2 已包含规则生成协议兼容性、规则卡片目标映射、API Key 本机凭据保存和页面文案回归；全量共运行 348 项测试。 | `.venv/bin/python -m unittest discover -s tests -q` 与 `.venv/bin/python run_demo.py --check` 均须通过；本轮新增专项测试结果记录在 2.5。后续任何解析、RAG、规则、报告或页面变更均须重跑；外部模型真机对比仍需有效 API Key、元数据外发审批和供应商侧用量核对。 |
| 阶段 5：Agent 增强 | v0.7-v1.1 本地能力与验收已完成 | v0.7-v0.8 完成规则编制与自然语言自定义规则，v0.9 完成标准依据 RAG，v0.9.1 完成 Harness，v1.0 完成显式工作流、同请求一次恢复和脱敏会话历史，v1.1 完成评估前规则生成、逐条澄清和规则文件批量导入。 | 服务端持久化、登录鉴权、跨用户权限和批量数据集评估仍属于后续生产化阶段；v1.1 的“批量”仅指同一数据集的规则文件整批生成。 |
| 阶段 6：联调与交付 | v1.2 已提交并进入专项测试 | 原有格式链路、43 项指标、报告口径和 v0.7-v1.1 Agent 链路均保持独立协议与证据绑定；v1.2 增加规则生成兼容性、目标指标安全投影、API Key 本机凭据保存和页面文案回归。 | v1.2 的 348 项基线测试、随机本机端口健康检查和中文规则联调已通过；提交 `08ef0f8` 已通过 GitHub 连接器推送到 `codex/v1.2`，草稿 PR #10 待评审。专项测试按 2.5 逐项收口后再宣称 v1.2 完成验收。 |
| 阶段 7：Agent Harness | 已完成（v0.9.1） | 已建立 29 个规则金标、6 个 RAG 金标、版本化 Prompt、Provider/Model 元数据、隐私化 Trace、异常输入和重复执行语义指纹。 | Schema、支持范围、字段映射、参数、确定性执行、重复执行语义、RAG 状态和引用有效率均为 100%；未审批执行和无依据标准声称均为 0。 |
| 阶段 8：可演示 Agent 闭环 | 已完成（v1.0） | 已实现显式状态转换、上下文/请求绑定、非法越级拒绝、人工审批、正式执行幂等、同请求一次失败恢复和最多 20 条脱敏会话历史。 | 从自然语言到确定性结果端到端通过；执行失败保留审批，改变请求后拒绝恢复，页面不会重复审批或重复执行。 |
| 阶段 9：评估前规则批量生成 | 已完成并上传（v1.1） | 已支持为 43 项目录指标填写补充规则、输入自然语言自定义规则，以及从 TXT、Markdown、CSV、Excel、JSON、JSONL、NDJSON 文件导入最多 100 条规则；逐条检查缺失字段、阈值、枚举、频率或比较条件，全批次通过后生成一个 RulePack。 | 任一规则待澄清或失败时不生成可审批 RulePack、不启动最终评估；全部通过后统一试运行和本地人工审批，再由确定性引擎正式执行。草稿 PR #9 待评审/合并。 |

### 2.1.1 v0.6 当前交付快照（2026-08-03）

下表优先于上方的 v0.5 历史快照；v0.6 的代码分支直接以 v0.4 为基线，不将 v0.5 的历史/比较/整改功能视为当前范围。

| 项目 | 状态 | v0.6 已完成 | 后续约束 |
| --- | --- | --- | --- |
| 指标目录 | 已完成 | 13 项基础指标与 30 项 DB31/T 指标共 43 项，均有稳定 ID、计算方式、简明含义和当前能力说明；同义项可并存，选择顺序规范化。 | 新增指标必须同步更新目录、计算器、报告协议、计算方式文档与测试。 |
| 严格可评估性 | 已完成 | `030300 数据重复率`、`030400 数据唯一性`可直接计算；需要外部标准、业务规则、权威参照、跨表关系、授权或访问上下文的项目如缺少依据即标记为无法评估。 | 不得将基础指标的启发式结果写成 DB31/T 标准符合性得分。 |
| 指标选择界面 | 已完成 | 选择区位于主内容区，统一展示 43 项卡片；默认/全选/清空/逐项选择可用；卡片右上角即时“？”提示指标含义，页面不显示来源、标准代码、层级或内部 ID。 | 保持选择集合和报告哈希的可复现性；提示文案必须来自指标目录。 |
| 页面状态流转 | 已完成 | 运行评估后自动隐藏选择区并显示结果；报告提供缺失标准明细和“补充评价标准”页面，用户可完成规则审批后重新评估。用户移除上传文件或替换文件时，自动清除报告、Agent 和 RulePack 状态，并恢复初始选择页。 | 任何新的页面流程不得保留已失效输入对应的旧报告或派生状态。 |
| 验收与交付 | 已完成 | `.venv/bin/python -m unittest discover -s tests` 通过 271 项；提交 `29374d6` 已推送到 GitHub `codex/v0.6`。 | 代码变更后需重跑完整测试；若改变指标选择/报告状态，还须补充 Streamlit `AppTest`。 |

界面命名已于 2026 年 7 月 21 日统一：浏览器页签和页面主标题均使用“政务数据集质量评估”，不再显示 “Demo”；项目目录及历史文档文件名保持不变，避免影响现有路径和追溯记录。

当前默认风险阈值已作为可调整工程配置实现，尚不代表政务业务统一标准。产品 `VERSION` 已更新为 `0.6`，指标选择目录版本为 `0.6`；v0.6 报告记录规范化后的选择集合，以保证同一输入、基准日期和指标集合可复现。v0.3 模型增强方案仍默认关闭；DeepSeek 不参与指标计算、风险判定、指标选择或 v0.4 RulePack 重评。模型调用成本预算、服务采购、部署方式和数据外发审批仍需单独确认。综合分继续后置，不作为 v0.6 的默认目标。

### 2.2 JSON 读取扩展路线（2026-07-21 启动）

对公开数据集下载清单中 13 个 JSON / GeoJSON 标注候选项进行实际结构审计后，扩展启动前的 Demo 对其中可确认样本的直接兼容数为 0。主要差距不是指标引擎，而是 JSON 数据的容器形态、编码、分片与嵌套结构。

| 扩展阶段 | 状态 | 范围 | 代表数据 | 验收口径 |
| --- | --- | --- | --- | --- |
| 阶段 A：常见表格型 JSON | 已完成并复审（2026-07-22） | 首行表头的二维数组；`data` / `rows` / `records` / `items` / `list` / `result` 常见接口包装；`.jsonl` / `.ndjson`；UTF-8 BOM、UTF-16 / UTF-32 BOM 与 GB18030；仅针对整体 `{[...]}` 外壳的定向修复。 | 温州平台二维数组、台州预览接口、深圳积涝点实际下载文件。 | 补齐大整数保真、包装元数据 Unicode、JSONL 严格空白/行号、自动处理 warning 保留，以及重复表头、错误值和歧义包装候选的有界错误文本；不改变既定形态范围。 |
| 阶段 B：安全压缩包与分片 | 已完成并复审（2026-07-22） | 安全读取 ZIP 中的 JSON / JSONL / NDJSON 分片，检查路径、普通文件类型、重复成员、符号链接、加密、Stored / Deflated 压缩白名单、条目数、展开体积、压缩比、分片表头和字段集合，按包内顺序从压缩流合并。 | 已以温州形态的 160 个二维数组分片建立回归；真实 800,000 条总量仍会按既定 200,000 条 JSON 上限明确拒绝。 | 损坏 Deflate 流统一转为可下载失败报告；下一分片在物化前受包级剩余记录/单元格/键值对/数组预算限制；冲突分片的必需 warning 不丢失。 |
| 阶段 C：GeoJSON 与有限嵌套映射 | 已完成并复审（2026-07-22） | 支持 `.geojson` 和 `.json` 中的顶层 FeatureCollection；每 Feature 一行，白名单为扁平 `properties`、字符串/数值 `Feature.id` 及 `geometry.type` 和坐标位置数、维度、二维范围摘要。 | `geojson_feature_collection.geojson` 演示样例；Sen1Floods11 等真实元数据需先满足扁平 properties 与几何白名单。 | 必需 properties / geometry、未定义嵌套路径、LineString 基数、Polygon 线性环闭合、空 GeometryCollection 和 ZIP 映射前隔离均已回归；不展开或导出坐标数组。 |

跨阶段安全原则：保留 50 MiB 单文件上限、JSON 200,000 条记录上限、全局行列/单元格/文本上限、整棵文档 Unicode 严格校验、重复键拒绝、非标准数值拒绝、大整数保真、嵌套深度预检和 ZIP 压缩方法白名单。阶段 A / B / C 不引入 JSON5、注释、逗号容错、异构分片静默合并、坐标全量展开或通用“猜测修复”，避免把非标准文件静默改成另一份数据。

### 2.3 Agent 化路线（v0.3-v0.5）

Agent 的目标不是增加一个聊天框，而是形成“理解目标 → 检查确定性报告 → 发现信息缺口 → 询问用户 → 提出建议或规则草案 → 经用户批准后重新评估 → 比较整改效果”的受控任务闭环。`QualityReport` 始终是指标与风险事实源；模型生成内容保存为独立对象，不回写或覆盖确定性报告。

| 版本 | 定位 | 核心工作 | 主要产出 | 版本验收门槛 |
| --- | --- | --- | --- | --- |
| v0.3 | 报告诊断 Agent（本地开发与验收已完成） | 已建立 DeepSeek 原生 Chat Completions 接入和受控工具层；确定性引擎为各指标生成完整疑似问题位置，并以独立 CSV 提供下载，位置列表不进入页面报告、结构化报告或模型上下文；Agent 只读消费白名单后的 `QualityReport`；提供报告概括、重点风险排序、指标解释、无法评估项说明、整改建议和基于报告的问答；页面提供快捷入口及对话区；模型不可用时使用模板回退。 | 独立 `AgentAnalysis` 结构；带证据引用的报告摘要；完整疑似问题位置 CSV；优先整改清单；问答记录；模型、提示词和工具调用审计信息。 | 本地 210 项回归和真实页面健康检查已通过；所有数值结论必须引用 `metric_key`、`risk_id` 或报告证据；不发送原始字段值或记录定位列表；Agent 不得修改确定性结果；DeepSeek 真机调用待有效 API Key 与外发审批后验收。 |
| v0.4 | 引导式规则 Agent（本地开发与验收已完成） | 已根据零配置报告画像生成本地业务信息缺口问题；支持确认最多 5 字段复合主键、必填字段、更新时间与日/周/月/季/年/自定义频率、允许值和数值闭区间；生成绑定报告哈希、输入哈希与基准日期的版本化 `RulePack` 和字段语义映射；经确定性校验及用户明确批准后，从当前上传字节重新执行原解析和零配置评估链，再追加业务规则结果并展示本次新增差异。 | 独立 `RulePack` 与 JSON Schema；规则来源、版本、本地自声明审批人、审批时间和草案哈希；独立 `RuleEvaluationResult` 与 JSON Schema；增强报告、差异说明和不含原始值的规则问题位置 CSV。 | 本地 247 项回归、固定报告一致性、`AppTest` 和真实页面健康检查已通过；未批准、非法、审批后修改或与当前输入不匹配的规则均被拒绝；人工确认绑定草案哈希；规则检查与问题位置有独立资源上限；原 13 项指标、既有风险、默认阈值和零配置报告保持不变；该 v0.4 验收点当时尚未包含跨历史改善判断。 |
| v0.5 | 整改闭环（本地功能与全量验收已完成） | 仅在用户显式保存当前报告或导入严格 `QualityReport` JSON 后写入当前会话内存；用户填写同一 `dataset_series_id` 并明确确认属于同一治理对象后，确定性比较器比较指标、风险、可评估性和字段结构变化；本地模板生成可分派任务、改进摘要和下一轮建议。 | 严格历史导入与版本趋势；独立 `ReportComparison`、`RemediationPlan`、`GovernanceRecord` 及 JSON Schema；比较 JSON、行动计划 JSON / Markdown / CSV 和治理记录 JSON 导出。 | 历史最多 20 份、单份 8 MiB、总计 32 MiB；只在当前本地浏览器会话可见，退出会话即失效，可显式删除，无登录鉴权；不保存原始上传、问题位置 CSV、Agent 输出或 RulePack。所有改善/恶化结论绑定两份固定报告哈希；指标方向、引擎/Schema/阈值兼容性、可评估性、未来日期和业务规则口径采用保守语义；模型不可用时全部功能不受影响。最终 282 项测试、固定报告一致性和真实本机健康检查均通过。 |

#### 2.3.1 v0.3 启动前置条件

| 前置项 | 最低完成要求 |
| --- | --- |
| 报告引用契约 | 为字段级指标建立唯一 `metric_key`；风险可精确引用指标；报告包含规则实际值、阈值、运算符和规则版本。 |
| 可复现上下文 | 记录报告哈希、引擎版本、评估基准日期、阈值配置版本、解析路径和必要运行信息。 |
| 结构化协议 | 发布 `QualityReport` JSON Schema 和独立 `AgentAnalysis` Schema；模型输出必须通过结构校验后才能展示或保存。 |
| 工具边界 | v0.3 模型只可调用 `get_report_summary`、`list_priority_risks`、`get_risk_evidence`、`get_metric_evidence`、`list_not_assessable` 五个只读工具；行动计划导出由本地确定性代码完成，不作为模型工具。v0.4 的 `validate_rule_pack` 是页面调用的本地无副作用受控入口，未注册为 DeepSeek 工具；`compare_reports` 仅属于 v0.5。 |
| 隐私与安全 | 默认仅发送结构化报告和必要元数据；任何原值或样例传递都需单独授权、脱敏和数量限制；字段名、单元格和上传内容一律视为不可信数据，防止提示注入。 |
| 模型运行策略 | 支持超时、一次重试、有界缓存、输入/输出 token 记录、模型切换和模板回退；token 用量作为成本核算依据，不在报告中固化易变的模型单价。 |
| 评测基线 | 建立固定报告和问答金标集；数字引用正确率与证据覆盖率目标为 100%，未经授权的确定性结果修改为 0，模型不可用时基础流程可用率为 100%。 |

#### 2.3.2 Agent 跨版本不变边界

- 大模型不重新计算 13 项指标，不修改风险等级，不生成未经批准的综合分。
- 大模型不静默展平、修复、清洗或覆盖原始数据。
- 大模型不能自行启用业务规则、修改默认阈值或改变记录粒度。
- 原始数据默认不发送到外部模型；如需使用脱敏样例，必须提供明确授权、用途、范围和审计记录。
- Agent 输出必须区分“报告事实”“基于事实的建议”和“缺少依据、无法判断”。
- `AgentAnalysis`、`RulePack`、`ReportComparison`、`RemediationPlan`、`GovernanceRecord` 与 `QualityReport` 分开保存；任何模型输出都不能覆盖确定性结果，任何任务分派也不能改变比较证据。

### 2.4 已批准并完成的 Agent 路线（v0.7-v1.0，更新于 2026-08-10）

本路线已由用户批准并完成实现、验收和独立分支上传。v0.9 的 GitHub PR #7 仍为 draft；v1.0 已以 `codex/v0.9` 为基线，在独立 `codex/v1.0` 分支累计包含 v0.9.1 Harness 和 v1.0 工作流，远端提交为 `6599a3a`，草稿 PR 为 #8，不覆盖既有 v0.8/v0.9 分支。

| 版本 / 阶段 | 预计周期 | 核心目标 | 主要交付物 | 验收门槛 |
| --- | --- | --- | --- | --- |
| 设计冻结 | 3–5 天 | 冻结 Rule DSL v0.2、RuleDraft 和 Agent 工作流协议 | 架构决策记录、JSON Schema、示例规则、迁移说明 | 现有 RulePack v0.1 样例仍可读取，评审通过后进入实现 |
| v0.7：指标评价依据 Agent | 1–2 周 | 在每个指标卡片下录入评价依据，并生成可确认规则草案 | 第一批已实现：评价依据输入栏、已有规则的“默认：……”预填、无规则指标空白输入、空依据启动阻断、初始页自定义模型 API 配置、缺失标准指标清单、独立“补充评价标准”页面、规则编译服务、规则预览、校验、试运行和确认后重评；外部模型普通消息兼容、文本回复兼容、严格失败提示和缺失输入提示已补齐 | 首批覆盖现有 5 类 RulePack 规则；缺少关键信息时进入澄清并列出字段/条件/频率/阈值；报告明确列出具体缺失标准及所需输入；用户补充后可完成“AI 解析—试运行—批准并重新评估”；填写兼容 Chat Completions API 后两个 Agent 均调用对应模型；外部模型失败不得生成模板替代结果；全量测试通过 |
| v0.8：自然语言自定义规则 | 1–2 周 | 已完成：用户用自然语言新增规则，并统一进入 RulePack | 已完成核心协议、确定性校验/执行、Provider 和页面入口；覆盖格式/长度/条件必填/跨字段规则 | v0.8 基线保留在远端 `codex/v0.8`；后续 v0.9 回归继续覆盖自定义规则链路 |
| v0.9：标准依据 RAG | 1–2 周 | 已完成（2026-08-09）：检索项目预置标准，再绑定规则证据；页面不提供用户上传/确认/摄取入口 | 预置文档分段、版本/命名空间筛选、确定性检索、冲突/无结果处理、条款引用、RuleEvidence/RuleDraft/RulePack 绑定和报告页 RAG 入口 | 每条绑定证据可定位到文档/章节/条款/chunk/页码（如有）和内容快照；无依据时不声称标准符合 |
| v0.9.1：Agent Harness | 已完成（2026-08-10） | 建立规则生成、检索、工具调用和功能回归体系 | 29 个规则金标、6 个 RAG 金标、模型对比入口、版本化 Prompt、隐私化 Trace 和异常输入测试 | 本地 Harness 全部通过；Schema、映射、参数、执行、语义一致性、RAG 状态与引用有效率均为 100%；模型不可用时基础评估仍可用 |
| v1.0：可演示 Agent 闭环 | 已完成（2026-08-10） | 完成可恢复、可解释的完整 Agent 体验 | 显式工作流、上下文/请求指纹、规则历史、一次失败恢复、幂等审批/执行、演示和文档 | 基线 320 项测试与真实 Streamlit 健康检查通过；从自然语言到确定性结果端到端稳定通过 |

#### 2.4.1 v0.7 第一批功能

| 模块 | 首批范围 |
| --- | --- |
| RuleDraft | 保存用户评价依据、AI 解释、规则类型、字段、参数、依据、假设和澄清问题 |
| Rule DSL | 沿用并扩展现有 `primary_key`、`required`、`update_freshness`、`allowed_values`、`numeric_range` 五类规则 |
| Agent 流程 | `collecting → compiling → draft → validated → dry_run_complete → awaiting_approval → approved → executed` |
| 模型权限 | 只生成规则草案；不能审批、执行、修改指标或修改风险 |
| 页面入口 | 每个指标卡增加“评价依据”输入；已有规则预填“默认：……”且可修改，无规则指标保持空白；选中指标缺少依据时提示具体指标并禁用评估；报告摘要列出需要补充标准的指标和所需输入；报告页提供“补充评价标准”页面、AI 解析依据入口、规则预览、澄清问题、试运行结果和“补充完成并重新评估”入口 |
| 失败处理 | 未配置 API 时允许本地模板作为暂行演示；已配置外部模型时，模型不可用、输出不合法或信息不足不得模板回退，必须显示调用/解析错误，并列出补充评价依据缺少的字段、条件或参数 |

#### 2.4.2 v0.8 自定义规则范围（已完成）

首批新增规则为格式/正则、字符长度、条件必填和跨字段比较；跨表参照、自动清洗、任意 Python/SQL 和数据写回暂不纳入。v0.8 作为 v0.9 的规则编制基线，模型仍只生成候选草案，审批、试运行和正式执行由本地确定性代码完成。

#### 2.4.3 RAG 与 Harness 原则

- RAG 首批只服务于标准、规范和数据字典检索，不替代确定性规则执行。
- 文档引用展示名称、版本、章节/条款和片段定位；标准冲突时提示用户确认。
- Harness 重点验证规则 Schema、字段映射、参数准确性、引用有效性、异常输入和重复执行一致性。
- v0.9 不强制引入 LangGraph、Dify 或向量数据库；先实现可独立测试的本地文档摄取、确定性检索和引用接口。
- 标准文档不是规则授权本身：项目预置语料只提供候选依据，用户仍需确认规则草案，确定性引擎才可以试运行和执行；页面不提供用户文档上传、来源确认或摄取。

#### 2.4.4 v0.7 第一批实现记录（2026-08-06）

- 新增 `RuleDraft`、`RuleSpec`、`RuleEvidence` 和 `rule-draft.schema.json`。
- 新增本地模板 Provider、可选兼容 Chat Completions Provider（含 DeepSeek）、自然语言规则编译服务和显式工作流状态机。
- 指标卡片新增“评价依据”输入；报告摘要新增缺失标准明细，报告页新增“补充评价标准”，支持补充依据、解析、校验、试运行、审批和确定性重评。
- 扩展 RulePack 来源以兼容 `user_natural_language`，保留现有 `local_guided` 规则包兼容性。
- 未配置 API 时本地模板仅作为暂行演示；配置外部模型后不允许回退模板，模型不参与审批、正式执行、指标计算或风险判定。
- 新增 v0.7 协议、服务、工作流和 Streamlit 测试；补充默认评价依据、空依据阻断、自定义模型 API Provider、普通文本/非 JSON 回复兼容、外部失败不回退、缺失标准明细和补充评价标准页面回归；全量测试由 284 项增加到 289 项并通过。

#### 2.4.5 v0.8 第一批实现记录（2026-08-06）

- 扩展 Rule DSL 和 RulePack Schema，新增 `regex_format`、`string_length`、`conditional_required`、`field_comparison`，保留 v0.7 RulePack 来源兼容性。
- 新增正则长度/输入长度与高回溯结构校验；新增字符长度、条件触发和跨字段比较的确定性指标、风险与问题位置摘要。
- 新增 `compile_custom_rule_draft` 和本地模板自然语言解析，支持自定义规则的澄清、拒绝、试运行、审批和上传重评。
- 页面新增“自定义规则（v0.8）”入口；跨表参照、任意 Python/SQL、自动清洗和数据写回明确不支持。
- 当前已补充 4 类新规则的协议、Schema、模板 Provider、审批重评回归；v0.8 作为 v0.9 基线保留。

#### 2.4.6 v0.9 标准依据 RAG 实现记录（2026-08-09）

- 新增 `src/rag/models.py`、`ingestion.py`、`retrieval.py`、`citations.py`，实现标准文档元数据、来源审核、文本层 Markdown/TXT/PDF 摄取、分段、确定性检索、版本/命名空间筛选和稳定引用。
- 新增 `rag-document.schema.json`、`rag-search-response.schema.json`，扩展 `rule-evidence.schema.json`、`rule-draft.schema.json` 和 `rule-pack.schema.json`，让检索片段可绑定到规则证据并进行内容快照校验。
- 页面在生成报告后提供“标准依据 RAG”页签，检索项目预置标准语料并继续绑定规则证据；移除初始页用户上传、来源审核和摄取控件。
- 标准检索无外部模型依赖；按关键词、标准号、版本、命名空间等确定性条件返回候选片段。无结果、来源未审核或标准版本冲突时明确阻止无依据绑定，不把检索不到依据解释为符合标准。
- 评价前已批准的文档和绑定在上传业务数据、首次评估时保留；更换已有报告输入或评价范围时，按当前输入清理失效的派生状态。当前数据仅存本地 Streamlit 会话内存，不提供跨会话知识库。
- 默认标准语料为 `DB31T_1523-2024_公共数据质量评价指标及计算方式.md`。扫描件/图片型 PDF、自动标准符合性判定、自动批准规则、LangGraph/Dify/向量数据库均不属于本版本。
- `.venv/bin/python -m unittest discover -s tests -p 'test*.py'` 通过 301 项；`.venv/bin/python run_demo.py --check` 通过全量测试和随机本机端口 Streamlit 健康检查。GitHub PR #7 已通过连接器更新到远端，当前仍为 draft。

#### 2.4.7 v0.9.1 与 v1.0 完成记录（2026-08-10）

- v0.9.1：已为规则生成、标准检索、引用绑定和工具调用建立金标集；记录 Prompt/Provider/Model 版本、检索参数、工具调用、失败轨迹和语义指纹；异常输入、冲突标准、无结果与重复执行一致性测试均已通过。
- v1.0：已在 Harness 通过后完成可恢复 Agent 闭环；目录指标和自定义规则统一进入“编译 → 校验 → 试运行 → 等待审批 → 审批 → 正式执行”，并增加请求绑定、非法越级拒绝、同请求一次恢复、幂等保护和有界脱敏历史。
- 发布：GitHub 连接器已从远端 `codex/v0.9` 的 `64c3c1b` 创建 v1.0 单一提交 `6599a3a`、分支 `codex/v1.0` 和草稿 PR #8；PR 共 1 个提交、22 个变更文件。由于本地旧 v0.9 基线不同，本地同批文件提交为 `f21463f`，`VERSION=1.0`。
- 本轮不引入数据库、跨会话恢复、登录鉴权、多租户、跨用户共享、LangGraph、Dify 或新的运行时依赖；这些能力如需建设须另立生产化计划。

### 2.5 v1.2 专项测试加固计划（2026-08-14）

本节把 v1.2 发布后的测试项纳入开发计划。P0 项必须在下一次版本验收前完成；P1 项作为交付前回归；P2 项在准备多人或长期运行时执行。除真实模型联调外，均应保留可重复的本地自动化证据，不以一次人工点击代替回归测试。

| 编号 | 优先级 | 测试范围 | 通过条件 | 状态 |
| --- | --- | --- | --- | --- |
| P0-01 | P0 | 规则卡片完整链路：必填、正则、数值范围分别生成草案、预检、审批、执行 | 目标目录指标从 `not_assessable` 变为确定性得分；`business_*` 审计指标保留；未审批不能执行 | 通过（本轮） |
| P0-02 | P0 | 卡片与 DSL 语义错配：例如“数据格式约束规范性”卡片生成必填规则 | 本地确定性校验拒绝或明确警告，不能静默把规则投影到错误指标 | 通过（新增指标-规则类型白名单与 RulePack 防线） |
| P0-03 | P0 | 审批失效：更换文件、文件名/工作表、基准日期、指标集合、卡片依据、模型或 API Key 后复用旧审批 | 草案、试运行、审批签名和增强结果全部失效；不能越过新一轮预检和审批 | 通过（新增模型名/API URL/已保存 Key 变化回归；审批与预评估状态均清理） |
| P0-04 | P0 | DeepSeek/兼容接口异常：401、403、429、5xx、超时、空响应、非法 JSON、拒绝 JSON 模式 | 给出脱敏、可理解的失败原因；外部模型模式不静默模板回退；基础评估仍可用；日志和页面不泄露 Key | 通过（模拟接口）；真实配额异常待有意授权后联调 |
| P0-05 | P0 | API Key 生命周期：保存、普通刷新、上传/替换文件、断开重连、重新打开、显式清除 | 保存后可恢复；清除后不再恢复；同会话状态不丢失；Key 不出现在命令行、页面错误、下载或历史中 | 通过（自动化+浏览器刷新恢复；真实 Key 未执行清除） |
| P0-06 | P0 | 多浏览器会话/多用户 Key 隔离 | 会话 A 的 Key、报告、规则和历史不会出现在会话 B；若本机 Keychain 共享不满足，必须标记部署限制 | 通过（会话隔离；已标记 macOS 同一系统用户 Keychain 共享限制） |
| P0-07 | P0 | 旧版 v1.1 RulePack（无 `metric_targets`）导入/执行 | 明确迁移为空、兼容读取或拒绝原因；不得静默改变目标指标 | 通过（缺失 `metric_targets` 明确拒绝） |
| P0-08 | P0 | DeepSeek 同一输入重复生成（每类规则至少 20 次） | 字段逐字保留、规则类型/参数/字段顺序正确；多规则不静默漏项；差异有可解释记录 | 部分通过（本地 9 类×20 次与多规则防线通过；真实 DeepSeek 待当前会话提供可用 Key） |
| P1-01 | P1 | 规则批量规模 1/10/50/100 条及第 1/50/100 条失败 | 有界耗时、内存稳定；失败项可定位；不生成部分 RulePack；页面不假死 | 通过（新增 60 秒批次预算；传输/超时异常不再重复发送兼容请求；1/10/50/100 成功、1/50/100 失败定位与原子拒绝；RulePack 证据上限同步为 300） |
| P1-02 | P1 | 中文字段和近似字段名：繁简体、空格、括号、标点、超长字段名 | 不猜测字段、不误绑定；无法确认时进入澄清 | 通过（精确字段逐字保留；繁简、空格、半/全角括号与连字符近似写法均澄清；超长字段仅截断展示名、不改 DSL 字段） |
| P1-03 | P1 | 文件和规则资源上限：50 MiB、20 万 JSON 记录、2,000 万单元格、200 万规则检查 | 超限给出明确拒绝；不截断、不崩溃、不产生半份报告 | 通过（实际 50 MiB 与 200,001 条 JSON 边界、20,000,000 单元格与 2,000,000 规则检查拒绝路径均回归；失败仅生成明确失败报告） |
| P1-04 | P1 | JSON/Markdown/CSV/问题位置 CSV/报告下载一致性 | 指标、风险、规则、审批哈希一致；不含 API Key、原始值或隐私字段 | 通过（规则增强 Markdown 新增 RulePack、规则 ID、审批/输入/报告哈希；与 JSON 交叉核对，下载不含原始值或测试密钥） |
| P1-05 | P1 | CSV/Excel 公式注入和危险文本导出 | `=SUM`、`+cmd`、`-cmd`、`@cmd` 等内容不会在导出或表格软件中变成公式执行 | 通过（基础和规则问题位置 CSV 均统一转义四类公式前缀） |
| P1-06 | P1 | RAG 依据：有效、冲突、过期、未审核、伪造 chunk | 只有有效且绑定的依据可进入规则；无结果/冲突时阻止无依据结论 | 通过（有效、冲突、无结果、未审核、过期及伪造 chunk 均有直接与 Harness 回归） |
| P1-07 | P1 | Python 3.10/3.11/3.12 与 macOS/Windows/Linux | 依赖安装、全量测试和随机端口健康检查均通过；差异有记录 | 部分通过（本机 macOS / Python 3.12 全量测试与随机端口健康检查通过；新增 GitHub Actions 3 系统 × 3 Python 矩阵，待推送后取得远端结果） |
| P1-08 | P1 | 真实上传格式：CSV 编码、XLS/XLSX、JSON、JSONL/NDJSON、GeoJSON | 解析、画像、评估、下载完整链路通过；错误文件有可解释提示 | 通过（CSV 编码、XLS/XLSX、JSON、JSONL/NDJSON、GeoJSON 的解析与 Streamlit 完整报告/下载链路回归通过） |
| P2-01 | P2 | 多请求并发、限流和模型成本/token 监控 | 不串状态；达到限流时可恢复；用量和失败可追踪 | 通过（进程内并发/每分钟限流、成功失败与 token/耗时汇总；页面可查看不含密钥的调用统计） |
| P2-02 | P2 | 提示注入、HTML/Markdown、Unicode 控制字符、超长输入 | 不改变系统规则、审批或执行权限；页面安全渲染；输入有界拒绝 | 通过（输入边界拒绝控制字符和超长文本；Markdown 展示/文件名安全化；注入无法越过审批或执行） |
| P2-03 | P2 | 长时间运行和重复上传的内存增长 | 资源可回收；不跨会话残留文件、Key、报告或规则 | 通过（规则对话保留窗口、附件总量限制、临时目录清理及会话隔离回归） |
| P2-04 | P2 | 窄屏、键盘操作、43 项指标卡片和长文本可用性 | 关键控件可见、可操作、无遮挡；错误和审批状态清晰 | 通过（43 项控件标签/长度边界回归；窄屏堆叠 CSS、键盘焦点和长文本断行防线） |
| P2-05 | P2 | 代理、TLS、离线恢复与服务重启 | 模型不可用时基础评估仍可用；恢复后不重复审批、不重复执行 | 通过（显式 TLS 校验/系统代理、离线基础评估、幂等执行和真实 Streamlit 重启健康检查） |

本轮专项执行记录：

- 基线：`.venv/bin/python -m unittest discover -s tests -p 'test*.py'`，354 项通过；`.venv/bin/python run_demo.py --check`（随机端口健康检查）通过。
- P0-01：使用 `sample_data/good_dataset.csv` 和模板 Provider 复现中文必填、正则、数值范围三条卡片规则，3 项目录指标均从 `not_assessable` 投影为确定性得分，3 个 `business_*` 指标保留。
- P0-02：新增 `METRIC_RULE_TYPE_ALLOWLIST`，在 RuleDraft 校验和 RulePack 校验双层拒绝语义错配；“数据格式约束规范性”绑定“service_name 为必填”现为 `invalid`，不生成或执行 RulePack；新增批量预检回归。
- P0-03：新增 Streamlit 回归：报告生成后修改模型名称或保存新 API Key 会清除基础报告、普通规则状态、预评估草案/审批/执行结果和 Agent 状态；评估前签名仍绑定生效模型配置；全量回归通过。
- P0-04：模拟 401、403、429、500、超时、空响应、非法 JSON 和不支持 JSON 模式；前 7 类均明确失败且不回退，JSON 模式降级后可成功解析，Key 不进入错误文案；未对真实账户制造 401/限流请求。
- P0-05：凭据库单元测试、页面保存/上传/清除回归均通过；当前浏览器新会话及刷新后均显示已保存状态。为避免删除用户正在使用的真实 Key，本轮未在浏览器点击真实“清除 API Key”。
- P0-06：两个独立 Streamlit `AppTest` 会话的 API Key、报告、规则状态和历史互不可见；页面与文档明确 macOS 当前操作系统用户的 Keychain 由该用户多个浏览器会话共享，Demo 不承诺登录级多用户隔离。
- P0-07：RulePack 校验器对缺少 `metric_targets` 的旧版 v1.1 JSON 返回固定拒绝原因，并确认其不能执行；现行 schema 同时拒绝缺失字段，未增加静默迁移或目标猜测。
- P0-08：本地模板 Provider 对 9 种规则各重复 20 次，字段、规则类型、参数和顺序语义指纹全部一致；新增 Provider 只返回第一条规则时的多规则澄清回归。当前命令行环境未发现保存的 Key 或 `DEEPSEEK_API_KEY`（只记录存在性，不读取或打印密钥），因此未执行真实 DeepSeek 的 180 次外呼，待配置可用 Key 后再做有意授权的真机记录。
- P2-01：新增 `src/model_runtime.py`。外部 Agent 与规则 Provider 在同一进程受并发和每分钟请求上限保护；成功、失败、拒绝、输入/输出 Token、累计耗时按安全的 provider/model 汇总，页面只展示汇总且不记录 Key、提示词、原始数据或模型文本。并发释放、限流窗口恢复和 token 统计回归通过。
- P2-02：规则描述、规则工具参数、工作流指纹、Agent 问题、模型地址/名称均拒绝不安全 Unicode 控制字符和既有长度上限外输入；文件名和 Markdown 展示替换双向控制字符。注入文本无法直接进入审批/执行；HTTP/解析错误也不再回显原始异常详情。
- P2-03：新增有界的规则对话会话状态：最多 20 条消息、5 个附件、附件合计最多 2 MiB，合并后的规则文本仍不超过 4,000 字符；超限或控制字符内容会以页面提示移除。重复 8 次上传的临时目录均在函数返回后消失；新 Streamlit 会话不继承前一会话对话或附件。
- P2-04：43 个指标卡片的复选框和“评价依据 / 补充规则”均有可访问标签与 4,000 字符边界；新增 768px 以下单列堆叠、长文本断行和帮助图标键盘焦点样式。`AppTest` 覆盖控件数量、标签和长文本；浏览器实际截图受本地 URL 自动化策略限制，未将其替代为不受控的浏览器操作。
- P2-05：默认 HTTP 客户端显式保持 `verify=True`、`trust_env=True`；模拟离线模型时基础报告保持可用且不被修改。已审批执行结果重复请求不再调用执行器，工作流历史明确仅为当前会话内存。重启旧的 8501 进程后，以当前代码重新启动；`.venv/bin/python run_demo.py --check` 在本机权限下通过 380 项测试和随机端口健康检查。

## 3. 优先级与依赖关系

| 优先级 | 内容 | 原因 |
| --- | --- | --- |
| P0：必须 | 文件解析、数据画像、13 项指标、三层结果、测试、可运行界面。 | 共同构成不依赖大模型也可展示的完整质量评估闭环。 |
| P1：本版本已完成 | v0.3 报告契约与只读诊断 Agent；v0.4 本地规则引导、RulePack 契约、显式审批与确定性重评；v0.5 历史比较整改；v0.7-v0.9 规则编制、自定义规则与标准依据 RAG；v0.9.1 Harness；v1.0 显式工作流、失败恢复和脱敏历史。 | 各版本均保持确定性事实与模型输出分离；比较、规则证据、标准引用、审批、执行与恢复均绑定固定对象，不能以模型输出覆盖确定性结果。 |
| P2：交付收尾 | v1.2 已通过 GitHub 连接器发布到独立 `codex/v1.2`，草稿 PR #10 已创建；v0.9-v1.1 分支保持不变。 | P0-01～P0-07 与 P2-01～P2-05 已完成修复和回归；本机权限下 380 项测试与随机端口健康检查通过。P0-08 的真实 DeepSeek 重复调用仍需有意授权后联调。 |
| P3：后续生产化 | 服务端持久化、身份认证与细粒度权限、跨用户共享、批量/周期性治理；另行完成既有 v0.3 DeepSeek 真机联调。 | 当前会话内存实现不承诺跨会话保存或多人生产治理能力，生产化另立阶段。 |

依赖顺序：先保持现有解析、指标、风险和网页闭环稳定；随后按“RuleDraft/DSL → v0.7 → v0.8 → v0.9 RAG → v0.9.1 Harness → v1.0”推进。AI 只参与评价依据理解和规则草案编制，不进入指标计算和风险判定链路。

## 4. 技术实现拆分

| 模块 | 建议实现 | 本期完成标准 |
| --- | --- | --- |
| 解析与指标层 | 使用 `pandas`、`xlrd`、`openpyxl` 读取 CSV、`.xls`、`.xlsx`、JSON；每项指标写成独立函数，统一返回 `value`、`status`、`evidence`、`reason`。 | 读取失败有原因；结果可复算；无数据时显示无法计算而非伪造 0。 |
| 规则层 | 根据字段画像决定可计算指标；根据默认阈值产生风险提示。 | 不依赖业务数据字典；阈值集中配置。 |
| Agent 与整改层 | v0.3 保留只读解释 `QualityReport`、DeepSeek 可选调用和模板回退；v0.4 由本地引导、审批和重评服务完成规则闭环；v0.5 由确定性比较器与本地模板生成 `ReportComparison`、`RemediationPlan` 和 `GovernanceRecord`，不调用 DeepSeek。 | 不能改写原始数据、指标、风险和无法评估项；数值与改善结论必须引用报告/比较证据；人工分派只能改变负责人、截止日期和任务状态，不能改变优先级或证据。 |
| 规则编制层 | v0.7 第一批已新增 `RuleDraft`、自然语言规则编译服务、澄清、试运行和用户确认；v0.8 扩展自定义 Rule DSL；v0.9 增加标准依据 RAG；v0.9.1 建立 Harness。 | AI 只生成结构化规则草案；规则校验、试运行和正式执行由本地确定性代码完成。 |
| 标准依据 RAG 层 | v0.9 提供项目预置标准文档的分段、关键词/标准号/版本/命名空间筛选、确定性检索、冲突与无结果处理、稳定引用和 RuleEvidence 绑定；页面不提供用户上传、来源审核或摄取。 | 每条绑定依据都能定位到文档、版本、章节/条款、chunk、页码（如有）和内容快照；冲突版本或无检索结果时不得声称标准符合；当前仅保存于本地会话内存。 |
| 历史与界面层 | `history_store.py` 只在当前会话内存保存经严格验证的固定报告；`app.py` 负责编排显式保存/导入、删除、趋势、同一治理对象确认、比较、任务分派与导出。 | 一条命令启动；历史容量和删除策略可见；未确认同一治理对象时不得比较；退出会话不承诺保留历史。 |

## 5. 交付前检查清单

| 检查项 | 通过条件 |
| --- | --- |
| 输入与核心质量 | CSV、Excel、JSON 至少各有一个样例通过；13 项指标按数据可用性输出数值或无法计算原因；原始文件不会被修改。 |
| 标准依据流程 | 先上传业务数据并运行评估；在报告页检索项目预置标准，按标准号/版本筛选，并在人工确认后绑定到规则证据。 |
| 结果边界 | 清晰区分确定性指标、风险提示和无法评估项；不把风险提示表述为确定错误。 |
| Agent 独立性 | 不配置模型 API 时，规则引擎、网页展示、报告下载、标准文档检索和本地模板 Agent 仍能完成完整流程；AI 输出不得覆盖确定性报告。 |
| Agent 证据约束 | Agent 展示的每个数值、风险等级、改善结论和标准依据都能定位到固定报告、指标、比较结果或已审核文档片段；无法定位时必须拒绝作答或标记无法判断。 |
| Agent 权限边界 | 原值外发和规则启用均采用显式授权；模型不能修改原始文件、确定性报告、默认阈值或未审批规则。 |
| Agent 故障回退 | 模型超时、限流、输出不合法、提示注入或 API 不可用时，不影响原有评估、展示、比较和报告下载。 |
| 历史与比较治理 | 只有显式保存/导入的固定报告进入会话内存；导入时校验严格 JSON、Schema、报告哈希和交叉引用；比较前必须填写同一 `dataset_series_id` 并确认属于同一治理对象。 |
| 整改记录可追溯性 | 任务及改进摘要绑定 `ReportComparison`；人工分派不改变证据和优先级；`GovernanceRecord` 绑定比较、计划、两份报告哈希与会话历史策略。 |
| 可运行与稳定 | 按 README 能完成上传、评估和报告下载；至少用 3 个样例连续跑通，无阻塞性报错。 |

## 6. 主要风险与兜底方案

| 风险 | 兜底方案 |
| --- | --- |
| 大模型 API 暂不可用或效果不稳定 | Agent 后置且可选；当前确定性报告与网页闭环不依赖模型。报告默认不导出原始字段样例；未来任何原值传递都需单独授权和脱敏设计。 |
| 模型编造数值或曲解风险 | 强制结构化输出和证据引用；展示前校验所有指标、风险和比较引用；无法对应确定性报告的内容不得作为结论展示。 |
| 数据内容触发提示注入 | 将字段名、单元格和上传文本作为不可信数据；默认不传原值；工具采用白名单、参数校验和最小权限，数据内容不得改变系统指令或授权状态。 |
| Agent 擅自启用规则或改变口径 | `RulePack` 必须经过确定性校验和用户显式批准；记录规则来源、版本与审批信息；模型没有直接修改配置的权限。 |
| 标准来源冲突或依据不足 | RAG 记录文档标题、标准号、版本、章节/条款、chunk 和内容快照；冲突版本、未审核来源或无结果时阻止无依据绑定，并要求用户确认或补充文档；不把检索结果自动解释为合规结论。 |
| 会话历史被误认为持久档案 | 页面明确提示仅当前本地浏览器会话有效；支持逐份删除和全部清空；生产化前不得把当前实现宣传为数据库、登录账户或跨用户审计系统。 |
| 不同治理对象或口径被错误比较 | 用户必须明确声明同一 `dataset_series_id` 并确认同一治理对象；比较器复核报告哈希、引擎/Schema、阈值/风险规则与业务规则签名，不兼容时输出“不可比较”而不是改善/恶化。 |
| 文件结构差异或后期时间不足 | 第一版仅支持常见表格型数据；无法展平的嵌套 JSON 返回原因。时间紧张时保留阶段 0-4，缩减更多图表、Excel 导出和 LLM 增强。 |
| 评分阈值难以统一 | 先输出指标与风险提示，不强制生成单一总分；阈值放入配置文件，方便后续讨论修改。 |

## 7. 每周固定动作

| 时间点 | 固定动作 | 目的 |
| --- | --- | --- |
| 周一 | 根据本周验收标准拆分任务，只新增本周必要事项。 | 避免需求持续膨胀，保证按阶段交付。 |
| 周三 | 用现有样例跑一次端到端或模块测试，记录问题。 | 尽早发现读取、指标和结果展示之间的衔接问题。 |
| 周五 | 对照本周验收标准更新状态，保存可运行版本并安排下一周。 | 让每周都有可回退的稳定版本，不把问题留到最后。 |
